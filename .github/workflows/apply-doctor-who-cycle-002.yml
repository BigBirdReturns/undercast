name: retired-doctor-who-cycle-002-v1-carrier

# Owner-authored rearm for the valid UTF-8 v2 writer on canonical main recovery.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: exit 1
