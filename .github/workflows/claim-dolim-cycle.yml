name: claim-dolim-cycle

on:
  push:
    branches: [agent/star-trek-gold-completion]
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: claim-dolim-cycle
  cancel-in-progress: false

jobs:
  claim:
    if: github.ref == 'refs/heads/agent/star-trek-gold-completion'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/star-trek-gold-completion
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Claim the exact capability-compatible Dolim task
        run: |
          set -euo pipefail
          mkdir -p .luna data/review cycle-artifact
          node scripts/corpus-ops.mjs validate
          node scripts/waterline.mjs gate --scope star-trek --operation claim --requested 1
          node scripts/autopilot.mjs next \
            --agent luna \
            --scope star-trek \
            --capability-profile text-vision \
            --task-id ap_dd881640f7a224feb5d15466 \
            --limit 1 \
            --selection-basis 'Second-desk gold-shard cycle 3: Commander Dolim is a source-reviewed physical-prosthetic role with explicit full-facial makeup evidence and is compatible with text and exact-image review.' \
            --lease-minutes 10080 \
            --now 2026-07-24T15:30:00.000Z \
            --out .luna/batch.json \
            --prompt .luna/PROMPT.md
          node --input-type=module <<'NODE'
          import {readFile,writeFile} from 'node:fs/promises';
          const batch=JSON.parse(await readFile('.luna/batch.json','utf8'));
          if(batch.tasks?.length!==1||batch.tasks[0].id!=='ap_dd881640f7a224feb5d15466') throw new Error('unexpected task leased');
          if(batch.tasks[0].character!=='Dolim'||batch.tasks[0].performer!=='Scott MacDonald') throw new Error('Dolim identity drifted');
          await writeFile('data/review/trek-cycle-003-dolim-lease.json',JSON.stringify(batch,null,2)+'\n');
          await writeFile('cycle-artifact/batch.json',JSON.stringify(batch,null,2)+'\n');
          await writeFile('cycle-artifact/PROMPT.md',await readFile('.luna/PROMPT.md','utf8'));
          NODE
          node scripts/autopilot.mjs validate
          node scripts/waterline.mjs status --scope star-trek --json > cycle-artifact/waterline-after-claim.json
      - uses: actions/upload-artifact@v4
        with:
          name: trek-cycle-003-dolim-lease
          path: cycle-artifact
          if-no-files-found: error
          retention-days: 30
      - name: Commit the durable lease and remove this one-shot workflow
        run: |
          set -euo pipefail
          rm -rf .luna cycle-artifact
          rm -f .github/workflows/claim-dolim-cycle.yml
          git config user.name 'undercast-cycle-operator'
          git config user.email 'undercast-cycle-operator@users.noreply.github.com'
          git add data/AUTOPILOT.json data/journal/autopilot.jsonl data/review/trek-cycle-003-dolim-lease.json .github/workflows/claim-dolim-cycle.yml
          git diff --cached --check
          git commit -m 'Cycle 3: persist the Dolim lease'
          git push origin HEAD:agent/star-trek-gold-completion
