name: estate-repair-collect008-staging-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded repair authority
        run: |
          echo 'This staging helper is retired.' >&2
          echo 'The NUL-safe candidate census is embedded in estate-adopt-current-null-lane-008-v2.' >&2
          exit 1
