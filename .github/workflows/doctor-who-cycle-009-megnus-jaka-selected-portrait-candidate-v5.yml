name: doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5

"on":
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - ".github/workflows/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5.yml"

permissions: {}

concurrency:
  group: doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5
  cancel-in-progress: true

env:
  EXACT_MAIN: a9b1a29098e36f03340ff6678b3edd06f1d1d3e2
  BRANCH: agent/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-a9b1-v5
  SELF: .github/workflows/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5.yml
  OUT: /tmp/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5
  RECOVERY_ROOT: /tmp/doctor-who-cycle-009-megnus-jaka-selected-portrait-recovery-v5
  RECOVERY_RUN: "31247667496"
  RECOVERY_JOB: "93078795883"
  RECOVERY_HEAD: 25f91d574fe94325944d321bccda26524e29263c
  RECOVERY_ARTIFACT: "9018992787"
  RECOVERY_ZIP_SHA256: c7725e2fe90470434ad729fc076ef194105c826b3c09270f7668667b04d88801
  SOURCE_MATERIALIZER_SHA256: fe4a0525c937ad7a996edf165e1db62b48c01f63ea79366ef690af21dc201d73
  PATCHED_MATERIALIZER_SHA256: 7f6a151b37708aab18e84decb7b2517aaaf82c4cc1eef5dd11a5bf24f631f922
  PORTRAIT_RUN: "31244912336"
  PORTRAIT_JOB: "93071806226"
  PORTRAIT_ARTIFACT: "9018175075"
  PORTRAIT_ZIP_SHA256: a9e5ff653c9f9656794f3210ef04669a37f13801ff3bd7ddc4dca53c41afe826
  PORTRAIT_REVIEW_IDENTITY: 4ba3aa3e07e160655bd66cafa36743d08d1626fec1e2b01126a9d8f9cc81e48e
  PORTRAIT_PRODUCER_IDENTITY: 1bfcf412a6ac5709b4505c38191cc68d6db586514397b23ed5ba23755e04aa09
  PORTRAIT_METADATA_SHA256: 26bb7c5e06356b6fac6db54708d412bddda297ef47b6a2b970c7c5ed0a9de8ed
  PORTRAIT_SHA256: 997a5b4ea0f849f78f7169b9de050c72ae7537339c77b1c61d52199fabe74d68
  PORTRAIT_BYTES: "51125"
  PORTRAIT_ORIGIN: "https://commons.wikimedia.org/wiki/File:Dan_Starkey_(27471533276).jpg"
  PREFLIGHT_RUN: "31237323043"
  PREFLIGHT_ARTIFACT: "9015893321"
  PREFLIGHT_ZIP_SHA256: 1a8ec1e3c248d93fccebe342b44eb1d4d2e4b8b069785309180c85d5343adf7c
  PREFLIGHT_IDENTITY: e530d17bac039c63278f5ae53c73d071b991e606a9eb4b3b74957f8ead1734b4
  EVIDENCE_RUN: "31239057687"
  EVIDENCE_ARTIFACT: "9016403859"
  EVIDENCE_ZIP_SHA256: 4f1665ad983e40525223539f6c1ad368f392c2d29887200cb3ee2ce4238d822f
  EVIDENCE_IDENTITY: dd59759b825c013869e5c7d75d99cdc12d2f38c4e46262e97a7aacd81bf7192c
  REVIEW_RUN: "31239209379"
  REVIEW_ARTIFACT: "9016447677"
  REVIEW_ZIP_SHA256: 3e62fc7f4224deb16d82efc9f117cc2c89d3dbe889668b6e70c3c9bffe39efec
  REVIEW_IDENTITY: b975d3e6f49a3e17f2c2c192eeb52029d4d8aae554a411ccf87c4295720e7a52
  TASK_ID: ap_45bb5fdcdf4aff65aa7a87c3
  EXPECTED_WALL_ID: UC-1354
  SOURCE_URL: https://tardis.fandom.com/wiki/Megnus_Jaka
  SOURCE_PAGEID: "175060"
  SOURCE_REVISION: "3677686"
  SOURCE_TIMESTAMP: "2023-12-19T13:57:50Z"
  SOURCE_SHA256: 6c4efd51f4972e4d754b2cd762386c8888098eed78fce8902838ed561f74fec4
  SOURCE_FINGERPRINT: 799004715f79b976ef967f63bfd4a085be22683047d87fcdf1dbc73968e50b35
  PRODUCT_URL: https://www.bigfinish.com/releases/v/doctor-who-the-first-sontarans-418
  CLAIM_AT: "2026-08-08T07:20:00.000Z"
  SUBMIT_AT: "2026-08-08T07:21:00.000Z"
  ACCEPT_AT: "2026-08-08T07:22:00.000Z"
  MERGE_AT: "2026-08-08T07:23:00.000Z"
  AUDIT_AT: "2026-08-08T07:23:30.000Z"
  REVIEW_AT: "2026-08-08T07:24:00.000Z"
  COMMIT_AT: "2026-08-08T07:25:00.000Z"
  FETCHED_AT: "2026-08-08"
  SELECTION_BASIS: Exact canonical cycle-009 preflight, revision-bound voice-performance evidence, official production evidence, exact cover refusal, and the independently reviewed full-census Dan Starkey portrait selected the deterministic Megnus Jaka obligation.

jobs:
  materialize:
    if: >-
      github.repository == 'BigBirdReturns/undercast' &&
      github.event_name == 'pull_request' &&
      github.event.pull_request.base.ref == 'main' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-a9b1-v5'
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    permissions:
      contents: read
      actions: read
      pull-requests: read
    env:
      GH_TOKEN: ${{ github.token }}
      PR_HEAD: ${{ github.event.pull_request.head.sha }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          filter: blob:none
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - name: Recover V4 proof, repair exact portrait schema, and execute
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$RECOVERY_ROOT"
          mkdir -p "$RECOVERY_ROOT"/{api,archive,extracted}
          test "$(git rev-parse HEAD)" = "$PR_HEAD"
          test "$PR_BASE_SHA" = "$EXACT_MAIN"
          test "$(git rev-list --count "$EXACT_MAIN"..HEAD)" = 1
          test -z "$(git rev-list --merges "$EXACT_MAIN"..HEAD)"
          test "$(git diff --name-only "$EXACT_MAIN"...HEAD)" = "$SELF"
          test "$(git diff --name-status "$EXACT_MAIN"...HEAD)" = $'A\t'"$SELF"
          test "$(gh api "/repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')" = "$EXACT_MAIN"

          gh api "/repos/$GITHUB_REPOSITORY/actions/runs/$RECOVERY_RUN" > "$RECOVERY_ROOT/api/run.json"
          gh api "/repos/$GITHUB_REPOSITORY/actions/jobs/$RECOVERY_JOB" > "$RECOVERY_ROOT/api/job.json"
          gh api "/repos/$GITHUB_REPOSITORY/actions/artifacts/$RECOVERY_ARTIFACT" > "$RECOVERY_ROOT/api/artifact.json"
          jq -e --argjson id "$RECOVERY_RUN" --arg h "$RECOVERY_HEAD" '.id==$id and .status=="completed" and .conclusion=="failure" and .head_sha==$h' "$RECOVERY_ROOT/api/run.json" >/dev/null
          jq -e --argjson id "$RECOVERY_JOB" --argjson run "$RECOVERY_RUN" '.id==$id and .run_id==$run and .status=="completed" and .conclusion=="failure"' "$RECOVERY_ROOT/api/job.json" >/dev/null
          jq -e --argjson id "$RECOVERY_ARTIFACT" --argjson run "$RECOVERY_RUN" --arg h "$RECOVERY_HEAD" --arg d "sha256:$RECOVERY_ZIP_SHA256" '.id==$id and .expired==false and .digest==$d and .workflow_run.id==$run and .workflow_run.head_sha==$h' "$RECOVERY_ROOT/api/artifact.json" >/dev/null
          gh api "/repos/$GITHUB_REPOSITORY/actions/artifacts/$RECOVERY_ARTIFACT/zip" > "$RECOVERY_ROOT/archive/recovery.zip"
          test "$(sha256sum "$RECOVERY_ROOT/archive/recovery.zip" | cut -d' ' -f1)" = "$RECOVERY_ZIP_SHA256"

          ROOT="$RECOVERY_ROOT" python3 - <<'PY'
          import hashlib, os, pathlib, re, zipfile

          root = pathlib.Path(os.environ['ROOT'])
          archive = root / 'archive/recovery.zip'
          target = root / 'extracted'
          with zipfile.ZipFile(archive) as z:
              infos = z.infolist()
              assert infos and len(infos) == len({i.filename for i in infos})
              for info in infos:
                  path = pathlib.PurePosixPath(info.filename)
                  assert not path.is_absolute() and '..' not in path.parts
                  if not info.is_dir():
                      output = target / path
                      output.parent.mkdir(parents=True, exist_ok=True)
                      output.write_bytes(z.read(info))

          wrapper = target / 'doctor-who-cycle-009-megnus-jaka-selected-portrait-wrapper-v4'
          script = wrapper / 'materialize.sh'
          assert script.is_file()
          source = script.read_bytes()
          assert hashlib.sha256(source).hexdigest() == os.environ['SOURCE_MATERIALIZER_SHA256']
          text = source.decode()
          old = """    'license': producer['license'],
              'licenseUrl': producer['license_url'],
              'focus': {'x': 0.5, 'y': 0.28},
              'pin': True,
          }"""
          new = """    'license': producer['license'],
              'focus': {'x': 'center', 'y': 'upper'},
              'pin': True,
          }"""
          assert text.count(old) == 1
          text = text.replace(old, new)
          assert "'licenseUrl':" not in text
          assert text.count("'focus': {'x': 'center', 'y': 'upper'}") == 1
          script.write_text(text)
          assert hashlib.sha256(script.read_bytes()).hexdigest() == os.environ['PATCHED_MATERIALIZER_SHA256']
          blocks = re.findall(r"python3 - <<'PY'\n(.*?)\nPY", text, re.S)
          assert len(blocks) == 10
          for index, block in enumerate(blocks, 1):
              compile(block, f'embedded-python-{index}', 'exec')
          portrait = wrapper / 'portrait'
          assert portrait.is_dir()
          pathlib.Path(root / 'materializer-path.txt').write_text(str(script) + '\n')
          pathlib.Path(root / 'portrait-root.txt').write_text(str(portrait) + '\n')
          PY

          script="$(cat "$RECOVERY_ROOT/materializer-path.txt")"
          export PORTRAIT_ROOT="$(cat "$RECOVERY_ROOT/portrait-root.txt")"
          bash -n "$script"
          chmod 700 "$script"
          exec "$script"
      - name: Upload exact selected-portrait candidate packet
        uses: actions/upload-artifact@v4
        with:
          name: doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5-${{ github.run_id }}-attempt-${{ github.run_attempt }}
          path: /tmp/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5
          if-no-files-found: error
          retention-days: 30
          compression-level: 0
      - name: Upload candidate diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5-diagnostics-${{ github.run_id }}-attempt-${{ github.run_attempt }}
          path: |
            /tmp/doctor-who-cycle-009-megnus-jaka-selected-portrait-candidate-v5
            /tmp/doctor-who-cycle-009-megnus-jaka-selected-portrait-recovery-v5
          if-no-files-found: ignore
          retention-days: 14
          compression-level: 0
