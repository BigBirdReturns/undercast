name: card-backfill-amortized-resume-after-staging-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED — successor routing is owned by card-backfill-supervisor.yml'
          echo 'Historical direct target card-backfill-amortized-wave.yml is retained only as a regression marker.'
          echo 'GitHub-token-authored pushes are not assumed to trigger successor workflows.'
          echo 'manual_continue_required=false'
