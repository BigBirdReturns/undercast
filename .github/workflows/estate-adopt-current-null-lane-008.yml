name: estate-adopt-current-null-lane-008-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded launch authority
        run: |
          echo 'This workflow is retired by estate-adopt-current-null-lane-008-v2.' >&2
          echo 'The V2 lane owns the NUL-safe 24-object smoke-first transaction.' >&2
          exit 1
