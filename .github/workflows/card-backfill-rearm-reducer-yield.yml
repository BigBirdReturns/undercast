name: card-backfill-rearm-reducer-yield

on:
  push:
    branches:
      - agent/card-backfill-next-041
    paths:
      - '.github/workflows/card-backfill-rearm-reducer-yield.yml'

permissions:
  contents: read
  actions: write
  statuses: write

jobs:
  rearm:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      REARM_HEAD: ${{ github.sha }}
    steps:
      - name: Dispatch exact-head supervisor
        run: |
          set -euo pipefail
          gh workflow run card-backfill-supervisor.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref "$GITHUB_REF_NAME" \
            -f reason=durable-reducer-yield-installed
          gh api -X POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${REARM_HEAD}" \
            -f state=success \
            -f context='card-backfill/rearm' \
            -f description='supervisor dispatched after durable reducer-yield installation' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          echo 'manual_continue_required=false'
