name: estate-adopt-current-null-lane-008-v2-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded 24-object authority
        run: |
          echo 'This workflow is retired after the COLLECT-008 cross-card duplicate gate.' >&2
          echo 'UC-323/still and UC-338/still resolve to byte-identical K9 media.' >&2
          echo 'A corrected 23-adoption plus one-deferment lane supersedes it.' >&2
          exit 1
