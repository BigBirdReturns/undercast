name: estate-adopt-card-backfill-uc046-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded single-record publication lane
        run: |
          echo 'This workflow is retired by COLLECT-003.' >&2
          echo 'Use .github/workflows/estate-adopt-card-backfill-packets.yml, which preserves historical invalid bytes and owns the bounded canonical adoption transaction.' >&2
          exit 1
