name: card-backfill-source-v3-wave-autonomous-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED — this legacy source-policy-v3 lane used the GitHub Models second desk.'
          echo 'All continuing discovery, local adjudication, reduction, publication, and re-entry are owned by card-backfill-amortized-wave.yml and card-backfill-supervisor.yml.'
          echo 'Any already-running legacy wave is artifact-only and must fail its exact-head reducer after this retirement commit advances the branch.'
          echo 'manual_continue_required=false'
