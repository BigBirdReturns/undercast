name: card-backfill-amortized-activation-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED — the retained 140-obligation wave is reduced by card-backfill-local-desk-recovery.yml.'
          echo 'Continuing production waves use the repository-local second desk.'
          echo 'GitHub Models and Copilot policy access are not campaign dependencies.'
          echo 'manual_continue_required=false'
