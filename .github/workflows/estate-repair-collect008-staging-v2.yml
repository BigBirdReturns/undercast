name: estate-repair-collect008-staging-v2-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse duplicate repair authority
        run: |
          echo 'This repair lane is retired.' >&2
          echo 'The sole COLLECT-008 authority is estate-adopt-current-null-lane-008-v2.' >&2
          exit 1
