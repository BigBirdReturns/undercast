name: inspect-pr56-main-integration

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: inspect-pr56-main-integration
  cancel-in-progress: true

jobs:
  inspect:
    if: github.event.pull_request.number == 58
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/luna-autopilot
          fetch-depth: 0
      - name: Attempt current-main merge and capture exact conflicts
        run: |
          test "$(git rev-parse HEAD)" = "0c77d15cdef111040c7bad7c2153df21616f4b02"
          git config user.name "undercast-integration"
          git config user.email "undercast-integration@users.noreply.github.com"
          git fetch origin main
          set +e
          git merge --no-commit --no-ff origin/main > /tmp/merge.out 2>&1
          status=$?
          set -e
          mkdir -p /tmp/pr56-main-integration
          cp /tmp/merge.out /tmp/pr56-main-integration/merge.out
          git status --short > /tmp/pr56-main-integration/status.txt
          git diff --name-only --diff-filter=U > /tmp/pr56-main-integration/conflicts.txt
          git diff --cc > /tmp/pr56-main-integration/conflicts.diff || true
          printf '%s\n' "$status" > /tmp/pr56-main-integration/merge-status.txt
          cat /tmp/pr56-main-integration/status.txt
          cat /tmp/pr56-main-integration/conflicts.txt
      - uses: actions/upload-artifact@v4
        with:
          name: pr56-main-integration-diagnostics
          path: /tmp/pr56-main-integration
          if-no-files-found: error
          retention-days: 3
