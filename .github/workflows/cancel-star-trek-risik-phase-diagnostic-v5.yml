name: cancel-star-trek-risik-finalizer-phase-diagnostic-v5

on:
  push:
    branches:
      - agent/star-trek-risik-finalizer-phase-diagnostic-v5

permissions:
  contents: read

concurrency:
  group: star-trek-risik-finalizer-phase-diagnostic-v5
  cancel-in-progress: true

jobs:
  cancel:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - run: echo "Superseded v5 diagnostic cancelled; bounded v6 owns the read."
