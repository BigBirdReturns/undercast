name: card-backfill-uc-060-discover

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - ".github/CARD-BACKFILL-UC-060.json"
      - "scripts/card-backfill-uc-060-discover.mjs"
      - ".github/workflows/card-backfill-uc-060-discover.yml"

permissions:
  contents: read

concurrency:
  group: card-backfill-uc-060-discover-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  discover:
    if: github.event.pull_request.head.ref == 'agent/card-backfill-next-009'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/card-backfill-next-009
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - name: Install canonical and browser runtime
        run: |
          set -euo pipefail
          npm ci
          npx playwright install --with-deps chromium
      - name: Install deterministic review renderer
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq imagemagick
          mkdir -p "$RUNNER_TEMP/bin"
          cat > "$RUNNER_TEMP/bin/magick" <<'SH'
          #!/bin/sh
          if [ "${1:-}" = "identify" ]; then shift; exec identify "$@"; fi
          exec convert "$@"
          SH
          chmod +x "$RUNNER_TEMP/bin/magick"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Verify exact UC-060 audit and discovery boundary
        run: |
          set -euo pipefail
          node --input-type=module - <<'NODE'
          import fs from 'node:fs';
          const c=JSON.parse(fs.readFileSync('.github/CARD-BACKFILL-UC-060.json','utf8'));
          const s=JSON.parse(fs.readFileSync('data/specimens.json','utf8')).find(r=>r.id==='UC-060');
          const a=JSON.parse(fs.readFileSync('data/MEDIA-AUDIT.json','utf8')).items.find(r=>r.id==='ma_6f881c41a40205bbf89d547a');
          if(c.version!==1||c.record_id!=='UC-060'||c.actor!=='Ray Park'||c.side!=='still')throw new Error('UC-060 control drift');
          if(!s||s.actor!=='Ray Park'||s.character!=='Darth Maul'||s.still)throw new Error('UC-060 canonical boundary changed');
          if(!a||a.wall_id!=='UC-060'||a.side!=='still'||a.status!=='absent'||a.expected_subject!=='Darth Maul'||a.asset)throw new Error('UC-060 audit boundary changed');
          if(c.sources?.length!==10)throw new Error('UC-060 source set drift');
          console.log('PASS — UC-060 remains a Darth Maul still absence');
          NODE
      - name: Gather and hash official Darth Maul candidates
        env:
          OUT: ${{ runner.temp }}/card-backfill-uc-060
          MAGICK_CMD: magick
        run: |
          set -euo pipefail
          node scripts/card-backfill-uc-060-discover.mjs 2>&1 | tee "$RUNNER_TEMP/card-backfill-uc-060.log"
          test -z "$(git status --porcelain)"
      - name: Upload exact candidates, pages and contact sheet
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: card-backfill-uc-060-discover-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/card-backfill-uc-060
            ${{ runner.temp }}/card-backfill-uc-060.log
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 90
