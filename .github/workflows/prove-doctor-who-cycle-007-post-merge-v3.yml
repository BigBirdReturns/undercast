name: prove-doctor-who-cycle-007-post-merge-v3

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - ".github/workflows/prove-doctor-who-cycle-007-post-merge-v3.yml"

permissions:
  contents: read
  actions: read
  pull-requests: read

concurrency:
  group: prove-doctor-who-cycle-007-post-merge-v3-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  PRODUCT_PARENT: c57a1f28b3f1998349fb2f87f847f6d240a43a3d
  PRODUCT_MAIN: e00b03436e354f3543cf4ed67a0c5b9f6ba4f97b
  EXACT_MAIN: 801d6129ab901e730ca8ec6333cebe0dd0d5574e
  PRODUCT_PR: "492"
  PRODUCT_HEAD: aa6138e707fed8562e69a747bfabc2a64dccefb2
  CLEANUP_PR: "499"
  CLEANUP_HEAD: 30bfe4f0be399cd02e5dceade22e5850acc06880
  CLEANUP_PATH: .github/workflows/doctor-who-cycle-007-v14-publisher.yml
  FINALIZER_HEAD: 5164bfc686a098c479a5fe578dda2ed75271e274
  FINALIZER_RUN: "31072356959"
  FINALIZER_JOB: "92522724079"
  FINALIZER_ARTIFACT_ID: "8956210712"
  FINALIZER_ARTIFACT_NAME: doctor-who-cycle-007-finalization-v14-31072356959
  FINALIZER_ARTIFACT_SHA256: 5aff21a35899ff35f0ee18dfeaaad74d51a4e4addcd0f70b53d74c0fefd23879
  FINALIZER_HANDOFF_SHA256: 18e1c3352bad9b881d879cbb9c2d900479dceff50e58e1461ceccc3bea490c04
  MEDIA_ASSET_SHA256: f6f32eb1c51445838ba27ae449548d9bb581b04bb2b321a5789f7f285872d409
  MEDIA_SEARCH_BLOB: 39c70aad4ae090b4a500c7dbfd525fe8a25930e8
  MEDIA_SEARCH_JOURNAL_BLOB: 17677703d71fe0f054887da6e2c8d9328dbab717
  CARRIER_BRANCH: agent/doctor-who-cycle-007-post-merge-proof-v3
  SELF: .github/workflows/prove-doctor-who-cycle-007-post-merge-v3.yml
  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
  HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
  ROOT: /tmp/doctor-who-cycle-007-post-merge-v3
  OUT: /tmp/doctor-who-cycle-007-post-merge-v3/out
  FINALIZATION: /tmp/doctor-who-cycle-007-post-merge-v3/finalization
  GH_TOKEN: ${{ github.token }}

jobs:
  prove:
    if: github.repository == 'BigBirdReturns/undercast' && github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'agent/doctor-who-cycle-007-post-merge-proof-v3'
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.HEAD_SHA }}
          fetch-depth: 0
          persist-credentials: false

      - name: Bind exact carrier, product commit, and publisher retirement
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$ROOT" "$OUT" "$FINALIZATION"
          test "$(git rev-parse HEAD)" = "$HEAD_SHA"
          test "$(git show -s --format=%P HEAD)" = "$EXACT_MAIN"
          test "$(git rev-list --count "$EXACT_MAIN"..HEAD)" = "1"
          test "$(git merge-base "$EXACT_MAIN" HEAD)" = "$EXACT_MAIN"
          test "$HEAD_BRANCH" = "$CARRIER_BRANCH"
          test -z "$(git status --porcelain)"
          printf '%s\n' "$SELF" > "$OUT/expected-carrier-paths.txt"
          git diff --name-only "$EXACT_MAIN"...HEAD | LC_ALL=C sort > "$OUT/actual-carrier-paths.txt"
          diff -u "$OUT/expected-carrier-paths.txt" "$OUT/actual-carrier-paths.txt"

          git fetch --no-tags origin "+refs/heads/main:refs/remotes/origin/main"
          test "$(git rev-parse refs/remotes/origin/main)" = "$EXACT_MAIN"
          test "$(git show -s --format=%P "$EXACT_MAIN")" = "$PRODUCT_MAIN"
          test "$(git rev-list --count "$PRODUCT_MAIN".."$EXACT_MAIN")" = "1"
          test "$(git merge-base "$PRODUCT_MAIN" "$EXACT_MAIN")" = "$PRODUCT_MAIN"
          printf 'D\t%s\n' "$CLEANUP_PATH" > "$OUT/expected-cleanup.txt"
          git diff --name-status "$PRODUCT_MAIN"..."$EXACT_MAIN" > "$OUT/actual-cleanup.txt"
          diff -u "$OUT/expected-cleanup.txt" "$OUT/actual-cleanup.txt"
          test ! -e "$CLEANUP_PATH"

          test "$(git show -s --format=%P "$PRODUCT_MAIN")" = "$PRODUCT_PARENT"
          test "$(git rev-list --count "$PRODUCT_PARENT".."$PRODUCT_MAIN")" = "1"
          test "$(git merge-base "$PRODUCT_PARENT" "$PRODUCT_MAIN")" = "$PRODUCT_PARENT"
          git diff --name-only "$PRODUCT_PARENT"..."$PRODUCT_MAIN" | LC_ALL=C sort > "$OUT/actual-product-paths.txt"
          test "$(wc -l < "$OUT/actual-product-paths.txt")" = "49"
          ! grep -E '^(\.github/workflows/|tmp/)' "$OUT/actual-product-paths.txt" >/dev/null
          git diff --check "$PRODUCT_PARENT"..."$PRODUCT_MAIN"

          for commit in "$PRODUCT_PARENT" "$PRODUCT_MAIN" "$EXACT_MAIN"; do
            test "$(git rev-parse "${commit}:data/MEDIA-SEARCH-LATEST.json")" = "$MEDIA_SEARCH_BLOB"
            test "$(git rev-parse "${commit}:data/journal/media-search.jsonl")" = "$MEDIA_SEARCH_JOURNAL_BLOB"
          done

          gh api "repos/${GITHUB_REPOSITORY}/pulls/${PRODUCT_PR}" > "$OUT/product-pr.json"
          jq -e --arg main "$PRODUCT_MAIN" --arg head "$PRODUCT_HEAD" \
            '.state=="closed" and .merged==true and .merge_commit_sha==$main and .head.sha==$head and .changed_files==49' \
            "$OUT/product-pr.json" >/dev/null

          gh api "repos/${GITHUB_REPOSITORY}/pulls/${CLEANUP_PR}" > "$OUT/cleanup-pr.json"
          jq -e --arg main "$EXACT_MAIN" --arg base "$PRODUCT_MAIN" --arg head "$CLEANUP_HEAD" \
            '.state=="closed" and .merged==true and .merge_commit_sha==$main and .base.sha==$base and .head.sha==$head and .changed_files==1 and .additions==0 and .deletions==303' \
            "$OUT/cleanup-pr.json" >/dev/null

      - name: Bind successful finalizer run, job, and immutable artifact
        shell: bash
        run: |
          set -euo pipefail
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${FINALIZER_RUN}" > "$OUT/finalizer-run.json"
          jq -e --arg head "$FINALIZER_HEAD" \
            '.status=="completed" and .conclusion=="success" and .head_sha==$head and .event=="pull_request"' \
            "$OUT/finalizer-run.json" >/dev/null

          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${FINALIZER_RUN}/jobs?per_page=100" > "$OUT/finalizer-jobs.json"
          jq -e --argjson job "$FINALIZER_JOB" \
            '.jobs | map(select(.id==$job)) | length==1 and .[0].name=="finalize" and .[0].status=="completed" and .[0].conclusion=="success" and all(.[0].steps[]; .conclusion=="success" or .conclusion=="skipped")' \
            "$OUT/finalizer-jobs.json" >/dev/null

          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${FINALIZER_RUN}/artifacts?per_page=100" > "$OUT/finalizer-artifacts.json"
          jq -e --argjson id "$FINALIZER_ARTIFACT_ID" --arg name "$FINALIZER_ARTIFACT_NAME" --arg digest "sha256:${FINALIZER_ARTIFACT_SHA256}" \
            '.artifacts | map(select(.id==$id)) | length==1 and .[0].name==$name and .[0].digest==$digest and .[0].expired==false' \
            "$OUT/finalizer-artifacts.json" >/dev/null

      - name: Download immutable V14 finalization custody
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.FINALIZER_ARTIFACT_NAME }}
          path: ${{ env.FINALIZATION }}
          github-token: ${{ github.token }}
          repository: BigBirdReturns/undercast
          run-id: ${{ env.FINALIZER_RUN }}

      - name: Rehash all 49 canonical product members on current main
        shell: bash
        run: |
          set -euo pipefail
          handoff="$(find "$FINALIZATION" -type f -name publisher-handoff.json -print -quit)"
          final_paths="$(find "$FINALIZATION" -type f -name expected-final-paths.txt -print -quit)"
          final_gate="$(find "$FINALIZATION" -type f -name final-gate.log -print -quit)"
          final_gate_sha="$(find "$FINALIZATION" -type f -name final-gate.sha256 -print -quit)"
          test -n "$handoff" -a -n "$final_paths" -a -n "$final_gate" -a -n "$final_gate_sha"
          test "$(jq -r '.receipt_sha256' "$handoff")" = "$FINALIZER_HANDOFF_SHA256"
          test "$(jq '.files | length' "$handoff")" = "49"
          test "$(sha256sum "$final_gate" | awk '{print $1}')" = "$(cat "$final_gate_sha")"
          LC_ALL=C sort "$final_paths" > "$OUT/expected-product-paths.txt"
          diff -u "$OUT/expected-product-paths.txt" "$OUT/actual-product-paths.txt"
          cp "$handoff" "$OUT/publisher-handoff.json"
          cp "$final_gate_sha" "$OUT/finalizer-gate.sha256"

          git checkout --detach "$EXACT_MAIN"
          test "$(git rev-parse HEAD)" = "$EXACT_MAIN"
          test "$(git show -s --format=%P HEAD)" = "$PRODUCT_MAIN"
          test ! -e "$CLEANUP_PATH"
          test -z "$(git status --porcelain)"

          HANDOFF="$handoff" node --input-type=module <<'NODE'
          import crypto from 'node:crypto';
          import fs from 'node:fs';
          const handoff = JSON.parse(fs.readFileSync(process.env.HANDOFF, 'utf8'));
          for (const [path, expected] of Object.entries(handoff.files)) {
            const actual = crypto.createHash('sha256').update(fs.readFileSync(path)).digest('hex');
            if (actual !== expected) throw new Error(`current main product member drifted: ${path}`);
          }
          NODE
          test "$(sha256sum images/uc-1352-still.jpg | awk '{print $1}')" = "$MEDIA_ASSET_SHA256"

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install exact current-main proof runtime
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npx playwright install --with-deps chromium
          sudo apt-get update >/dev/null
          sudo apt-get install -y imagemagick >/dev/null
          identify -version | head -1 | tee "$OUT/imagemagick-version.txt"

      - name: Verify structured Kragar task, lease, wall, media, and chronology
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import assert from 'node:assert/strict';
          import fs from 'node:fs';

          const records = JSON.parse(fs.readFileSync('data/specimens.json', 'utf8'));
          const row = records.find((item) => item.id === 'UC-1352');
          assert.ok(row, 'UC-1352 missing');
          assert.equal(row.actor, 'Dan Starkey');
          assert.equal(row.character, 'Kragar');
          assert.equal(row.universe, 'Doctor Who');
          assert.equal(row.kind, 'face');
          assert.equal(row.still?.src, 'images/uc-1352-still.jpg');
          assert.equal(Object.hasOwn(row, 'portrait'), false);

          const receipt = JSON.parse(fs.readFileSync('data/review/adapter-sdk/doctor-who-cycle-007-kragar.json', 'utf8'));
          assert.equal(receipt.task?.id, 'ap_ffea809b980b468b33fa462c');
          assert.equal(receipt.task?.performer, 'Dan Starkey');
          assert.equal(receipt.task?.character, 'Kragar');
          assert.equal(receipt.task?.adjudicated_kind, 'face');
          assert.equal(receipt.task?.adjudicated_performance_mode, 'physical-prosthetic');
          assert.equal(receipt.lease?.id, 'lease_acb2309e7fb477769749751c');
          assert.equal(receipt.lease?.outcome, 'completed');
          assert.equal(receipt.canonical?.wall_id, 'UC-1352');
          assert.equal(receipt.media?.statuses?.portrait, 'absent');
          assert.equal(receipt.media?.statuses?.still, 'verified');
          assert.equal(receipt.boundary?.still_sha256, 'f6f32eb1c51445838ba27ae449548d9bb581b04bb2b321a5789f7f285872d409');
          assert.equal(receipt.boundary?.generic_sontaran_image_used, false);
          assert.equal(receipt.boundary?.existing_dan_starkey_portrait_duplicated, false);
          NODE

          node scripts/doctor-who-cycle-006-chronology-composable.mjs | tee "$OUT/cycle-006-chronology.log"
          npm run doctor-who:cycle-007:check | tee "$OUT/cycle-007.log"
          node scripts/publisher-custody.mjs check-workflows | tee "$OUT/publisher-custody.log"
          node scripts/publisher-custody-or-guard.mjs --self-test | tee "$OUT/publisher-or-guard-self-test.log"
          node scripts/publisher-custody-or-guard.mjs | tee "$OUT/publisher-or-guard.log"

      - name: Run complete current-main repository and rendered gate
        shell: bash
        run: |
          set -euo pipefail
          npm run gate | tee "$OUT/complete-gate.log"
          test -z "$(git status --porcelain)"
          sha256sum "$OUT/complete-gate.log" | awk '{print $1}' > "$OUT/complete-gate.sha256"

      - name: Rebind live current main after the complete gate
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "+refs/heads/main:refs/remotes/origin/main"
          test "$(git rev-parse refs/remotes/origin/main)" = "$EXACT_MAIN"
          gh api "repos/${GITHUB_REPOSITORY}/pulls/${PRODUCT_PR}" > "$OUT/product-pr-after-gate.json"
          gh api "repos/${GITHUB_REPOSITORY}/pulls/${CLEANUP_PR}" > "$OUT/cleanup-pr-after-gate.json"
          jq -e --arg main "$PRODUCT_MAIN" '.state=="closed" and .merged==true and .merge_commit_sha==$main' "$OUT/product-pr-after-gate.json" >/dev/null
          jq -e --arg main "$EXACT_MAIN" '.state=="closed" and .merged==true and .merge_commit_sha==$main' "$OUT/cleanup-pr-after-gate.json" >/dev/null

      - name: Seal self-hashed current-main proof receipt
        shell: bash
        run: |
          set -euo pipefail
          job_id="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}/jobs?per_page=100" --jq '.jobs[] | select(.name=="prove") | .id' | head -1)"
          test -n "$job_id"
          gate_sha="$(cat "$OUT/complete-gate.sha256")"
          jq -n \
            --arg transaction "DOCTOR-WHO-CYCLE-007-KRAGAR-CURRENT-MAIN-PROOF-V3" \
            --arg verdict "current-main-verified" \
            --arg product_parent "$PRODUCT_PARENT" \
            --arg product_main "$PRODUCT_MAIN" \
            --arg main "$EXACT_MAIN" \
            --arg carrier "$HEAD_SHA" \
            --arg cleanup_path "$CLEANUP_PATH" \
            --argjson product_pr "$PRODUCT_PR" \
            --argjson cleanup_pr "$CLEANUP_PR" \
            --arg product_head "$PRODUCT_HEAD" \
            --argjson run "$GITHUB_RUN_ID" \
            --argjson job "$job_id" \
            --argjson finalizer_run "$FINALIZER_RUN" \
            --argjson finalizer_job "$FINALIZER_JOB" \
            --argjson finalizer_artifact "$FINALIZER_ARTIFACT_ID" \
            --arg finalizer_artifact_sha256 "$FINALIZER_ARTIFACT_SHA256" \
            --arg finalizer_handoff_sha256 "$FINALIZER_HANDOFF_SHA256" \
            --arg gate_sha256 "$gate_sha" \
            --arg media_sha256 "$MEDIA_ASSET_SHA256" \
            '{
              schema_version: 1,
              transaction: $transaction,
              verdict: $verdict,
              product_parent: $product_parent,
              product_main: $product_main,
              current_main: $main,
              carrier_head: $carrier,
              product_pr: $product_pr,
              cleanup_pr: $cleanup_pr,
              product_head: $product_head,
              product_commits: 1,
              product_paths: 49,
              cleanup_commits: 1,
              cleanup_paths: [$cleanup_path],
              surviving_workflow_paths: 0,
              surviving_transport_paths: 0,
              run_id: $run,
              job_id: $job,
              finalizer: {
                run_id: $finalizer_run,
                job_id: $finalizer_job,
                artifact_id: $finalizer_artifact,
                artifact_sha256: $finalizer_artifact_sha256,
                handoff_sha256: $finalizer_handoff_sha256
              },
              complete_gate_sha256: $gate_sha256,
              media_asset_sha256: $media_sha256,
              checks: {
                product_path_census_exact: true,
                publisher_retirement_exact: true,
                all_current_product_members_rehashed: true,
                current_main_observation_blobs_preserved: true,
                structured_task_modality_custody_passed: true,
                cycle_006_chronology_passed: true,
                cycle_007_checker_passed: true,
                publisher_custody_passed: true,
                complete_repository_and_rendered_gate_passed: true,
                live_main_rebound_after_gate: true
              },
              boundary: {
                repository_written: false,
                current_main_mutated: false,
                eighth_lease_issued: false,
                outside_human_dependency: false
              }
            }' > "$OUT/current-main-proof.json"
          sha256sum "$OUT/current-main-proof.json" | awk '{print $1}' > "$OUT/current-main-proof.sha256"
          test "$(sha256sum "$OUT/current-main-proof.json" | awk '{print $1}')" = "$(cat "$OUT/current-main-proof.sha256")"

      - name: Upload immutable current-main proof
        uses: actions/upload-artifact@v4
        with:
          name: doctor-who-cycle-007-current-main-proof-v3-${{ github.run_id }}
          path: ${{ env.OUT }}
          if-no-files-found: error
          retention-days: 30
          compression-level: 0
