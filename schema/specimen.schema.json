{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bigbirdreturns.github.io/undercast/schema/specimen.schema.json",
  "title": "UNDERCAST specimen",
  "description": "One performer record from data/specimens.json — a person who vanished under a designed face, mask, suit, mo-cap rig or an unseen voice. Field shape inferred from the 1067 live records and the fields index.html actually renders.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "character",
    "actor",
    "production",
    "universe",
    "years",
    "designer",
    "transform",
    "knownFor",
    "reveal",
    "link"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Catalog number. Used as the DOM data-uid, the permalink hash and the sort key. The optional 'G' marks a model-grown card (rendered with a Grown badge).",
      "pattern": "^UC-G?\\d+$"
    },
    "character": {
      "type": "string",
      "description": "The designed character shown on the front of the card.",
      "minLength": 1
    },
    "actor": {
      "type": "string",
      "description": "The performer underneath, revealed on the back. Also the A–Z sort key.",
      "minLength": 1
    },
    "production": {
      "type": "string",
      "description": "The film / series / show the character appears in.",
      "minLength": 1
    },
    "universe": {
      "type": "string",
      "description": "The shelf the specimen files under. Drives the shelf filter chips.",
      "enum": [
        "Star Trek",
        "Film",
        "TV",
        "Voice",
        "Kaiju",
        "Horror",
        "Babylon 5",
        "Farscape"
      ]
    },
    "years": {
      "type": "string",
      "description": "Free-form run of years (e.g. \"1993–99\", \"2001\"). The first 4-digit year is bucketed into a decade chip.",
      "minLength": 1
    },
    "designer": {
      "type": "string",
      "description": "Free-form credit line for who built the face; parsed into browsable maker names.",
      "minLength": 1
    },
    "transform": {
      "type": "integer",
      "description": "How far the performer vanished, 1 (barely) to 5 (unrecognisable). Rendered as five pips.",
      "minimum": 1,
      "maximum": 5
    },
    "knownFor": {
      "type": "string",
      "description": "A \"you've also seen/heard them\" note on the card back.",
      "minLength": 1
    },
    "reveal": {
      "type": "string",
      "description": "The payoff paragraph on the card back.",
      "minLength": 1
    },
    "link": {
      "type": "string",
      "description": "Outbound \"see the real face\" link.",
      "format": "uri",
      "pattern": "^https?://"
    },
    "kind": {
      "type": "string",
      "description": "Optional. \"voice\" swaps the relief art for a soundwave glyph and relabels the card. Absent means a face performer (the implicit default). NOTE: only \"voice\" occurs in the current data; \"face\" is the implied default index.html falls back to and is never written out.",
      "enum": [
        "face",
        "voice"
      ]
    },
    "still": {
      "description": "Real character still shown on the card front, or null / absent to fall back to the generated relief art.",
      "oneOf": [
        { "$ref": "#/$defs/image" },
        { "type": "null" }
      ]
    },
    "portrait": {
      "description": "Real actor portrait shown on the card back, or null / absent to fall back to the bare-face relief art.",
      "oneOf": [
        { "$ref": "#/$defs/image" },
        { "type": "null" }
      ]
    },
    "conditions": {
      "type": "array",
      "description": "Optional evidence-scoped physical or environmental performance conditions. Never infer these from an image; every entry requires a source URL.",
      "items": { "$ref": "#/$defs/condition" },
      "uniqueItems": true
    },
    "references": {
      "type": "array",
      "description": "Optional claim-level fact references. These support the performance, design, production and biographical text; image provenance remains in still/portrait and SOURCES.json.",
      "items": { "$ref": "#/$defs/reference" },
      "minItems": 1,
      "maxItems": 12,
      "uniqueItems": true
    },
    "_grown": {
      "type": "boolean",
      "description": "Optional. Marks a specimen added by the in-page replicator; renders a hatched front and a \"Grown\" badge. Not present in the current committed data."
    },
    "_verified": {
      "type": "boolean",
      "description": "Optional. Whether a grown specimen has been verified; controls the badge style. Not present in the current committed data."
    }
  },
  "$defs": {
    "reference": {
      "type": "object",
      "additionalProperties": false,
      "required": ["claim", "label", "source"],
      "properties": {
        "claim": {
          "type": "string",
          "enum": ["performance", "design-build", "production", "biography", "interview"]
        },
        "label": {
          "type": "string",
          "description": "Human-readable description of what this source supports.",
          "minLength": 4,
          "maxLength": 140
        },
        "source": {
          "type": "string",
          "format": "uri",
          "pattern": "^https://"
        },
        "publisher": {
          "type": "string",
          "minLength": 2,
          "maxLength": 80
        }
      }
    },
    "condition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "scope", "note", "source"],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "athletic-performance",
            "cold",
            "constrained-mobility",
            "extended-application",
            "full-body-enclosure",
            "heat",
            "restricted-breathing",
            "restricted-vision",
            "stunt-movement",
            "suspension-or-harness",
            "voice-strain",
            "water",
            "other"
          ]
        },
        "scope": {
          "type": "string",
          "enum": ["record", "production", "episode", "scene"]
        },
        "episode": {
          "type": "string",
          "minLength": 1
        },
        "note": {
          "type": "string",
          "minLength": 12
        },
        "source": {
          "type": "string",
          "format": "uri",
          "pattern": "^https?://"
        }
      }
    },
    "image": {
      "type": "object",
      "description": "A still (front) or portrait (back) image reference. index.html only reads .src and .kind; .origin/.author/.license/.pin are provenance carried alongside.",
      "additionalProperties": false,
      "required": [
        "src",
        "kind",
        "origin"
      ],
      "properties": {
        "src": {
          "type": "string",
          "description": "Image path, relative to the site root (e.g. \"images/uc-001-still.jpg\"). Not a URL.",
          "minLength": 1
        },
        "kind": {
          "type": "string",
          "description": "Provenance class of the image. still.kind is always \"still\"; portrait.kind is \"copyright\" or \"free\". \"generated\" is understood by the renderer (draws a \"gen\" badge) but is not used in the current data.",
          "enum": [
            "still",
            "generated",
            "copyright",
            "free"
          ]
        },
        "origin": {
          "type": "string",
          "description": "Source page the image was fetched from.",
          "format": "uri",
          "pattern": "^https?://"
        },
        "author": {
          "type": "string",
          "description": "Optional credited author. May be an empty string when unknown/unneeded (e.g. copyright stills)."
        },
        "license": {
          "type": "string",
          "description": "Optional licence label. May be an empty string when unknown/unneeded."
        },
        "pin": {
          "type": "boolean",
          "description": "Optional. Marks a curated/locked image choice. Only ever true in the current data."
        },
        "year": {
          "type": "integer",
          "description": "Optional. Year the photograph was taken, when known — provenance carried on 168 portraits. Not rendered; kept for citation/dating.",
          "minimum": 1800,
          "maximum": 2100
        }
      }
    }
  }
}
