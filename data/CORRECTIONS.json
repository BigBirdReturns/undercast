{
  "version": 1,
  "schema": "schema/correction.schema.json",
  "semantics": "Append-only registry of real correction cases after privacy screening. Empty means no admitted public correction case exists, never that the archive is error-free or that response time is zero.",
  "cases": []
}
