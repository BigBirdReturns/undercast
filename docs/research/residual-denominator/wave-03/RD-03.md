# RD-03 · Office of Strategic Capital instrument lifecycle and recovery

## Status

This draft is a repository-native reconstruction of the supplied Wave-03 prepublication contract. The original archive bytes were not mounted in the publication runtime, so this object preserves the supplied denominators and authority boundary without claiming byte identity with the unavailable archive.

```text
review status:              draft intake only
frozen units:               5
matrix fields:              11
required matrix cells:      55
official routes:            5
candidate routes:           45
fixed routes:               50
adversarial refusals:       36
external contacts:          0
external reviews:           0
outside-human dependency:   false
classes closed:             0
publication effect:         none
adoption effect:            none
graph effect:               none
```

## Fixed execution boundary

Every expanded route is cryptographically bound by ordered route-ID and route-URL SHA-256 values in the protocol. Expansion is unit-major, field-minor, variant-minor and cannot depend on a response.

```text
method:                     GET
maximum attempts:           1
timeout:                    45,000 ms
maximum body:               5 MiB
concurrency:                2
result-spawned followups:   0
automatic second pass:      false
```

A successful candidate-search response remains a candidate census. It cannot become evidence without a separate exact-capture transaction binding the official object, unit identity, event class, chronology, and bytes.

## Closure law

Announcement, conditional commitment, financial close, cash disbursement, performance, default, repayment, loss, and public recovery remain distinct and must join on the same legal instrument.

## Exact permanent paths

```text
data/research/residual-denominator/wave-03/rd-03/protocol.json
schema/rd-wave03-rd03-intake.schema.json
scripts/rd-wave03-rd03-build.mjs
scripts/rd-wave03-rd03-validate.mjs
test/rd-wave03-rd03-adversarial.mjs
.github/workflows/rd-wave03-rd03-intake.yml
docs/research/residual-denominator/wave-03/RD-03.md
```

## Qualification commands

```bash
node scripts/rd-wave03-rd03-validate.mjs
node scripts/rd-wave03-rd03-build.mjs --check
node test/rd-wave03-rd03-adversarial.mjs
```

The hosted pull-request workflow additionally verifies the exact branch head and path denominator, executes the bounded public-source protocol, terminally classifies every cell, verifies zero admissions and zero closures, and uploads the receipt without repository write authority.
