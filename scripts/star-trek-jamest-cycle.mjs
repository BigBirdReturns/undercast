#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-jamest-cycle.json", CHECKER="scripts/star-trek-jamest-cycle.mjs";
const TASK="ap_161a46a1338535d5f5b1c466", LEASE="lease_d57c444931eca0f2a51dcb8d", WALL="UC-1372", CYCLE="cycle_8e1ef98bc2b760f78837abb6", EVENT="waterline_427e9c449c922c95289ff243", REVIEW="2026-08-15T05:37:02.308Z";
const PERFORMER="Dee Bradley Baker", ROLE="James'T", SOURCE="https://memory-alpha.fandom.com/wiki/James'T", EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/All_the_World%27s_a_Stage_(episode)", FINGERPRINT="7ffeb74f2482bdbcd39f17aa60bd72493e1d5d17d111bae83c99f4090b9d3bfe";
const MAIN="7d5414dd67d80bc351787ac8399b28cf655a9b3b", CANDIDATE="25980ec9a7529b9159efcea9a933ce490ff39af3", TREE="09ccd17772f1678112717de95a6f5c8d0bf8f9d9", PATH_SHA="819bf51e779198f67efa8bcfeee03d0eb4b945d897ac199cb4e260d158558ba1", REVIEW_ID="e56acf380d0e249ae2ff889d67391f12184be85a19eddd9a09eaf9651d9789a4", REVIEW_FILE="68e8fc8a344cd47451edcfa0bc2565b8b577a6765bcc0c99b685c9bf81d98791", PUBLICATION_RUN=31867335216, PUBLICATION_JOB=94970382399;
const STILL_PATH="images/uc-1372-still.webp", STILL_SHA="3197209fe25ffb71bde31fecf063c5c6aade5e0e18e1f339d891478213988273", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/b/bf/James%27T.jpg/revision/latest?cb=20221110174027&path-prefix=en", PORTRAIT_PATH="images/uc-1372-portrait.jpg", PORTRAIT_SHA="51bdd06a6b1cf28be5581e85c3ae506bc78ec067b469713260a78e29a139d367", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Dee_Bradley_Baker_(53771747217)_cropped.jpg";
const GW_RECEIPT="data/review/adapter-sdk/star-trek-gwyndala-cycle.json", GW_RECEIPT_FILE="b1dbc993be4ba82d1453ff3440333b26808ee266671dd1424931e3be32abee2e", GW_RECEIPT_ID="669dd45c3061c0a2d5d050ff1cad5ad883110fe02ce09e6faa1a96f0f2036a4a", GW_CHECKER="scripts/star-trek-gwyndala-cycle.mjs", GW_CHECKER_SHA="e20105c59be0ef7446d310ac4cedbd9be66fbb5fd4468d98ad4598fdd48e41ed", GW_CYCLE="cycle_48c3fe84a11dfac325ba0f84";
const TOTAL=2228, RESOLVED_FLOOR=403;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-JAMEST'&&receipt.canonical_parent===MAIN,'JamesT receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'JamesT checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'JamesT task drifted');
same(task.performance_modes,['voice-animation'],'JamesT queued mode hint drifted');
same(task.wall_ids,[WALL],'JamesT wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'JamesT task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='Star Trek: Prodigy'&&card.universe==='Star Trek'&&card.years==='2022'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'JamesT canonical record drifted');
ok(card.reveal.includes('voiced James’T')&&card.reveal.includes('voice-only performance claim')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing'),'JamesT performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===EPISODE_SOURCE),'JamesT source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'JamesT media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 2.0','JamesT media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'JamesT source still drifted');
same(source.portrait,card.portrait,'JamesT source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'JamesT facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','JamesT still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','JamesT portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Enderprizian'&&receipt.task?.episode==='All the World’s a Stage'&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'JamesT adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'JamesT receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'JamesT claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'JamesT candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const jamest=byLease.get(LEASE);
ok(jamest?.id===CYCLE&&jamest.outcome==='completed'&&jamest.task_statuses?.[TASK]==='resolved'&&jamest.reviewed_at===REVIEW,'JamesT cycle receipt drifted');
const cb=structuredClone(jamest);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'JamesT cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'JamesT waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'JamesT waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1824,resolved:403,blocked:0,rejected:1,in_flight:0},'JamesT terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(GW_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(GW_RECEIPT))===GW_RECEIPT_FILE&&prior.receipt_sha256===GW_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===GW_CYCLE&&sha(fs.readFileSync(GW_CHECKER))===GW_CHECKER_SHA,'JamesT lost prior Gwyndala custody');
const priorRun=spawnSync(process.execPath,[GW_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Gwyndala checker failed under JamesT custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'JamesT qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'JamesT execution custody drifted');
ok(read('package.json').scripts?.['star-trek:jamest-cycle:check']==='node scripts/star-trek-jamest-cycle.mjs','JamesT checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'JamesT authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1372/'),'JamesT permanent route missing');
console.log('star-trek-jamest-cycle: PASS — exact James’T voice custody, exact independent character and performer media, no physical, animation, or maker promotion, reviewed waterline closure, immutable Gwyndala custody, and later-cycle bounds are intact');
