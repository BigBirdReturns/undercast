#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-kol-tai-cycle.json", CHECKER="scripts/star-trek-kol-tai-cycle.mjs";
const TASK="ap_9e3a49dc256ff237dd30611b", LEASE="lease_74117719e6571930deaa271f", WALL="UC-1389", CYCLE="cycle_a2b0baa44a5fbbdc9c2ddb6b", EVENT="waterline_604e54bfbc58774887085364", REVIEW="2026-08-19T22:17:20.024Z";
const PERFORMER="James Doohan", ROLE="Kol-Tai", SOURCE="https://memory-alpha.fandom.com/wiki/Kol-Tai", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Albatross_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Albatross_(episode)", FINGERPRINT="0476f56a4959105661f22140254bf364c76c4ace7ac513c111233c75e99cf9d2";
const MAIN="31832ca882b9b8d2a76f4a7f2d679844d7bde5ba", CANDIDATE="9cc411d37b3fc17625b9b05a68f2153d3066a97a", TREE="42a26c5293e405fba71b5d80e4a76c2726d84b20", PATH_SHA="cb34d06c3fa8818ea8253f4eb4d8eb4f9739eb7fe042da2ba8217df207a7ef32", REVIEW_ID="729418f3dd128b9110cf79ed97979cf2c62f0c95e39451364e3963bbdbcf741e", REVIEW_FILE="a76800d0110ed6b35ed52209371e00611bca375222df74666b367ce49441eb4e", PUBLICATION_RUN=32307780974, PUBLICATION_JOB=96244196141;
const STILL_PATH="images/uc-1389-still.webp", STILL_SHA="43296f967280910c45ca04744b65ec0d4fcd168b4b60e91fd8bd57e137cb0058", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/a/ac/Kol-Tai.jpg/revision/latest?cb=20061128011223&path-prefix=en", PORTRAIT_PATH="images/uc-1389-portrait.jpg", PORTRAIT_SHA="1223d998bc176e3bb879503576e4ed9b10cc6a15bcaa6ab459a1ef083ef01456", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Star_Trek_Cast_and_Crew_Visit_NASA Goddard Space Flight Center_Dryden_in_1967.jpg";
const PRIOR_ARI_RECEIPT="data/review/adapter-sdk/star-trek-karl-four-cycle.json", PRIOR_ARI_RECEIPT_FILE="de8d87037c5d33fd8ee36017d30833e714d86f7b5609ff3850921134ec755221", PRIOR_ARI_RECEIPT_ID="1d3ed948c177b9d4c10d8310c0293ffd583b0fe4731434b66b17c1e78aa00eca", PRIOR_ARI_CHECKER="scripts/star-trek-karl-four-cycle.mjs", PRIOR_ARI_CHECKER_SHA="6b1503d1d451c0ace67967d9e36cd1d75b23a7980b700c7e07bb030d803a8ed3", PRIOR_ARI_CYCLE="cycle_14480d3d96aae6be53784b13";
const TOTAL=2228, RESOLVED_FLOOR=421;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-KOL-TAI'&&receipt.canonical_parent===MAIN,'Kol-Tai receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Kol-Tai checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Kol-Tai task drifted');
same(task.performance_modes,['voice-animation'],'Kol-Tai queued mode hint drifted');
same(task.wall_ids,[WALL],'Kol-Tai wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Kol-Tai task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==="Star Trek: The Animated Series (Albatross)"&&card.universe==='Star Trek'&&card.years==='1974'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Kol-Tai canonical record drifted');
ok(card.reveal.includes('identifies James Doohan in Kol-Tai’s voice-performer field')&&card.reveal.includes('voice-only performance claim for Kol-Tai')&&card.reveal.includes('Karl Four')&&card.reveal.includes('Cadmar')&&card.reveal.includes('Cheeron')&&card.reveal.includes('Ari bn Bem')&&card.reveal.includes('role-specific still')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing')&&card.reveal.includes('production-shop'),'Kol-Tai performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'Kol-Tai source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Kol-Tai media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='Public domain','Kol-Tai media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Kol-Tai source still drifted');
same(source.portrait,card.portrait,'Kol-Tai source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Kol-Tai facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Kol-Tai still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Kol-Tai portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Dramen'&&receipt.task?.episode==="Albatross"&&receipt.task?.performance_scope==="James Doohan's voice performance as Kol-Tai in Albatross (1974)"&&receipt.task?.karl_four_role_not_conflated===true&&receipt.task?.domar_role_not_conflated===true&&receipt.task?.chuft_captain_role_not_conflated===true&&receipt.task?.cadmar_role_not_conflated===true&&receipt.task?.role_still_is_character_evidence_only===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Kol-Tai adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Kol-Tai receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Kol-Tai claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Kol-Tai candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const unitkoltai=byLease.get(LEASE);
ok(unitkoltai?.id===CYCLE&&unitkoltai.outcome==='completed'&&unitkoltai.task_statuses?.[TASK]==='resolved'&&unitkoltai.reviewed_at===REVIEW,'Kol-Tai cycle receipt drifted');
const cb=structuredClone(unitkoltai);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Kol-Tai cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Kol-Tai waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Kol-Tai waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1806,resolved:421,blocked:0,rejected:1,in_flight:0},'Kol-Tai terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(PRIOR_ARI_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(PRIOR_ARI_RECEIPT))===PRIOR_ARI_RECEIPT_FILE&&prior.receipt_sha256===PRIOR_ARI_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===PRIOR_ARI_CYCLE&&sha(fs.readFileSync(PRIOR_ARI_CHECKER))===PRIOR_ARI_CHECKER_SHA,'Kol-Tai lost prior Karl Four custody');
const priorRun=spawnSync(process.execPath,[PRIOR_ARI_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Karl Four checker failed under Kol-Tai custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Kol-Tai qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Kol-Tai execution custody drifted');
ok(read('package.json').scripts?.['star-trek:kol-tai-cycle:check']==='node scripts/star-trek-kol-tai-cycle.mjs','Kol-Tai checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.voice_credit_promoted_to_processing_credit===false&&receipt.boundary?.separate_role_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Kol-Tai authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1389/'),'Kol-Tai permanent route missing');
console.log('star-trek-kol-tai-cycle: PASS — exact Kol-Tai voice custody, exact independent character and performer media, no physical, animation, processing, or maker promotion, separate Karl Four, Cadmar, Cheeron, and Ari bn Bem custody, reviewed waterline closure, immutable Karl Four cycle custody, and later-cycle bounds are intact');
