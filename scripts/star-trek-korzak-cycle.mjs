#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-korzak-cycle.json", CHECKER="scripts/star-trek-korzak-cycle.mjs";
const TASK="ap_ffc961bf65110f75fb59a5e6", LEASE="lease_2f8588f26a9ee250a17aa77d", WALL="UC-1380", CYCLE="cycle_8181c2b2e74528a322aed6a0", EVENT="waterline_300a968b383478ce88fc1a80", REVIEW="2026-08-16T13:09:51.113Z";
const PERFORMER="Fred Tatasciore", ROLE="Korzak", SOURCE="https://memory-alpha.fandom.com/wiki/Korzak", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Hear_All,_Trust_Nothing_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Hear_All,_Trust_Nothing_(episode)", FINGERPRINT="ce2491a1f5c22621babedf0439e00ec8646a209104bfc6982a6f708df2b80764";
const MAIN="4fd2bce80e5ee48e345b263326911daae160ed7d", CANDIDATE="11904dd4332d68197649e27705cdb677b911df57", TREE="10a6ad6f3efd89867c0b668117602ad86eceee5f", PATH_SHA="89e6b9c33897142c51bf277ce92282b7be79e02260af5f3fb68657b39fe48f95", REVIEW_ID="2d150d7bd196e860266373bf6def932684a4d8482ee2563a820b171320a6ee16", REVIEW_FILE="3a755a48e9e3efb5506a733ea62e7bb6912ed5bb003fbac3b108936ceee6bebd", PUBLICATION_RUN=31948993191, PUBLICATION_JOB=95169406061;
const STILL_PATH="images/uc-1380-still.webp", STILL_SHA="52a400df8262686be3701f525c6fd2753b18fb0dbd06f49509f0002febe5b70a", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/9/9e/Korzak.jpg/revision/latest?cb=20221001140957&path-prefix=en", PORTRAIT_PATH="images/uc-1380-portrait.jpg", PORTRAIT_SHA="af073e0f1da80aad784baec953b9b788468e0ea600121cc5ab792efb98af7b3b", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:2023_San_Diego_Comic-Con_International_by_Gage_Skidmore,_251.jpg";
const PRIOR_KERNER_HAUZE_RECEIPT="data/review/adapter-sdk/star-trek-kerner-hauze-cycle.json", PRIOR_KERNER_HAUZE_RECEIPT_FILE="4258da971776dcbf4dc413e56c0e61aff97b75d188f88540db3f2db0c0f9bc77", PRIOR_KERNER_HAUZE_RECEIPT_ID="912b4e627047bc3f67e6720bc41f7a499e58cad7af5ec6cf81b2b259f1e668b4", PRIOR_KERNER_HAUZE_CHECKER="scripts/star-trek-kerner-hauze-cycle.mjs", PRIOR_KERNER_HAUZE_CHECKER_SHA="62908ebdb3e42477d97e85b2c39c6157d8e777d0d807edddee3537fae30a087c", PRIOR_KERNER_HAUZE_CYCLE="cycle_25e85fa6db3f904043118413";
const TOTAL=2228, RESOLVED_FLOOR=411;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-KORZAK'&&receipt.canonical_parent===MAIN,'Korzak receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Korzak checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Korzak task drifted');
same(task.performance_modes,['voice-animation'],'Korzak queued mode hint drifted');
same(task.wall_ids,[WALL],'Korzak wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Korzak task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==="Star Trek: Lower Decks (Hear All, Trust Nothing)"&&card.universe==='Star Trek'&&card.years==='2022'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Korzak canonical record drifted');
ok(card.reveal.includes('Fred Tatasciore in the actor field')&&card.reveal.includes('voice-only performance claim for Korzak')&&card.reveal.includes('Korax')&&card.reveal.includes('K’Levin')&&card.reveal.includes('Kerner Hauze')&&card.reveal.includes('role-specific still')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing')&&card.reveal.includes('production-shop'),'Korzak performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'Korzak source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Korzak media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 2.0','Korzak media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Korzak source still drifted');
same(source.portrait,card.portrait,'Korzak source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Korzak facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Korzak still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Korzak portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Karemma'&&receipt.task?.episode==="Hear All, Trust Nothing"&&receipt.task?.performance_scope==="Fred Tatasciore's voice performance as Korzak in Hear All, Trust Nothing (2022)"&&receipt.task?.korax_role_not_conflated===true&&receipt.task?.klevin_role_not_conflated===true&&receipt.task?.kerner_hauze_role_not_conflated===true&&receipt.task?.trade_minister_role===true&&receipt.task?.role_still_is_character_evidence_only===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Korzak adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Korzak receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Korzak claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Korzak candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const unitkorzak=byLease.get(LEASE);
ok(unitkorzak?.id===CYCLE&&unitkorzak.outcome==='completed'&&unitkorzak.task_statuses?.[TASK]==='resolved'&&unitkorzak.reviewed_at===REVIEW,'Korzak cycle receipt drifted');
const cb=structuredClone(unitkorzak);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Korzak cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Korzak waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Korzak waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1816,resolved:411,blocked:0,rejected:1,in_flight:0},'Korzak terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(PRIOR_KERNER_HAUZE_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(PRIOR_KERNER_HAUZE_RECEIPT))===PRIOR_KERNER_HAUZE_RECEIPT_FILE&&prior.receipt_sha256===PRIOR_KERNER_HAUZE_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===PRIOR_KERNER_HAUZE_CYCLE&&sha(fs.readFileSync(PRIOR_KERNER_HAUZE_CHECKER))===PRIOR_KERNER_HAUZE_CHECKER_SHA,'Korzak lost prior Kerner Hauze custody');
const priorRun=spawnSync(process.execPath,[PRIOR_KERNER_HAUZE_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Kerner Hauze checker failed under Korzak custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Korzak qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Korzak execution custody drifted');
ok(read('package.json').scripts?.['star-trek:korzak-cycle:check']==='node scripts/star-trek-korzak-cycle.mjs','Korzak checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.voice_credit_promoted_to_processing_credit===false&&receipt.boundary?.separate_role_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Korzak authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1380/'),'Korzak permanent route missing');
console.log('star-trek-korzak-cycle: PASS — exact Korzak voice custody, exact independent character and performer media, no physical, animation, processing, or maker promotion, separate Korax, K’Levin, and Kerner Hauze custody, reviewed waterline closure, immutable Kerner Hauze cycle custody, and later-cycle bounds are intact');
