#!/usr/bin/env node
import { chromium } from '@playwright/test';
import { createHash } from 'node:crypto';
import { execFileSync } from 'node:child_process';
import { mkdir, readFile, readdir, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';

const CONTROL='.github/CARD-BACKFILL-UC-054.json';
const OUT=process.env.OUT||'/tmp/card-backfill-uc-054';
const UA='Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/132 Safari/537.36 UNDERCAST-card-backfill/1.0';
const sha=value=>createHash('sha256').update(value).digest('hex');
const norm=value=>String(value||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[’‘]/g,"'").replace(/[^a-zA-Z0-9']+/g,' ').trim().toLowerCase();
const slug=value=>norm(value).replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'unknown';
const esc=value=>String(value||'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const assert=(condition,message)=>{if(!condition)throw new Error(message)};
const readJson=async path=>JSON.parse(await readFile(path,'utf8'));
const writeJson=async(path,value)=>{await mkdir(dirname(path),{recursive:true});await writeFile(path,JSON.stringify(value,null,2)+'\n')};
function signatureMime(bytes){if(bytes.length>=3&&bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff)return'image/jpeg';if(bytes.length>=8&&bytes.subarray(0,8).equals(Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a])))return'image/png';if(bytes.length>=12&&bytes.subarray(0,4).toString('ascii')==='RIFF'&&bytes.subarray(8,12).toString('ascii')==='WEBP')return'image/webp';return'unknown'}
function extensionFor(mime){return mime==='image/jpeg'?'jpg':mime==='image/png'?'png':mime==='image/webp'?'webp':'bin'}
function identify(path){const text=execFileSync(process.env.MAGICK_CMD||'magick',['identify','-format','%w %h',path],{encoding:'utf8'}).trim();const[width,height]=text.split(/\s+/).map(Number);assert(width>0&&height>0,`cannot identify ${path}`);return{width,height}}
function magick(...args){execFileSync(process.env.MAGICK_CMD||'magick',args,{stdio:'inherit'})}
async function walkImages(root,out=[]){let entries;try{entries=await readdir(root,{withFileTypes:true})}catch{return out}for(const entry of entries){const path=join(root,entry.name);if(entry.isDirectory())await walkImages(path,out);else if(/\.(?:jpe?g|png|webp)$/i.test(entry.name))out.push(path)}return out}
async function repositoryHashes(){const map=new Map();try{const manifest=await readJson('data/media-manifest.json');for(const[path,row]of Object.entries(manifest.assets||{})){if(!/^[0-9a-f]{64}$/i.test(row?.sha256||''))continue;const list=map.get(row.sha256)||[];list.push(`manifest:${path}`);map.set(row.sha256,list)}}catch{}for(const path of await walkImages('images')){try{const hash=sha(await readFile(path));const list=map.get(hash)||[];list.push(`file:${path}`);map.set(hash,list)}catch{}}return map}
function addCandidate(list,seen,candidate){if(!candidate?.url||!candidate?.source_page)return;let url;try{url=new URL(candidate.url,candidate.source_page).href}catch{return}if(!/^https?:/.test(url)||seen.has(url))return;seen.add(url);list.push({...candidate,url})}
function scoreImage(row,source){const hay=norm([row.alt,row.context,row.url,source.provider,source.key].join(' '));let score=0;if(hay.includes('michael keaton'))score+=45;if(hay.includes('beetlejuice'))score+=45;if(hay.includes('betelgeuse'))score+=45;if(/striped|stripe|graveyard|moss|mold|green hair|bio exorcist|ghost with the most/.test(hay))score+=25;if(row.width>=1200||row.height>=900)score+=10;if(row.width>=700||row.height>=500)score+=5;if(/logo|icon|avatar|author|newsletter|advert|banner|favicon|sprite|placeholder|tracking|pixel|poster|cover|trailer|merch|toy|figure/.test(hay))score-=100;if(/winona ryder only|alec baldwin only|geena davis only|catherine o hara only|jenna ortega/.test(hay))score-=70;return score}

async function inspectPage(context,source){const page=await context.newPage();try{await page.goto(source.source_page,{waitUntil:'domcontentloaded',timeout:90000});await page.waitForTimeout(1400);const body=await page.locator('body').innerText().catch(()=>''),html=await page.content(),hay=norm(body+' '+html),missing=(source.required_terms||[]).filter(term=>!hay.includes(norm(term)));const screenshot=`pages/${source.key}.png`;await mkdir(join(OUT,'pages'),{recursive:true});await page.screenshot({path:join(OUT,screenshot),fullPage:true});const images=await page.evaluate(()=>{const absolute=value=>{try{return new URL(value,document.baseURI).href}catch{return''}};const rows=[];for(const selector of['meta[property="og:image"]','meta[property="og:image:secure_url"]','meta[name="twitter:image"]','meta[name="twitter:image:src"]'])for(const meta of document.querySelectorAll(selector))rows.push({url:absolute(meta.content),alt:'',width:0,height:0,context:document.title,kind:'metadata'});for(const image of document.images){const values=[image.currentSrc,image.src,image.dataset.src,image.dataset.lazySrc,image.dataset.original,image.dataset.image].filter(Boolean);for(const part of String(image.srcset||image.dataset.srcset||'').split(',')){const value=part.trim().split(/\s+/)[0];if(value)values.push(value)}const figure=image.closest('figure');const caption=figure?.querySelector('figcaption')?.textContent||'';const parent=image.parentElement;const nearby=[image.alt,caption,parent?.getAttribute('aria-label')||'',parent?.textContent?.slice(0,500)||'',document.title].filter(Boolean).join(' ').replace(/\s+/g,' ').slice(0,1200);for(const value of values)rows.push({url:absolute(value),alt:image.alt||'',width:image.naturalWidth||image.width||0,height:image.naturalHeight||image.height||0,context:nearby,kind:'page-image'})}return rows});return{title:await page.title(),resolved_url:page.url(),status:'loaded',required_terms_missing:missing,body_text:body.slice(0,7000),screenshot,images}}catch(error){return{status:'error',error:error.message,images:[]}}finally{await page.close()}}
async function download(context,candidate,index){let response;try{response=await context.request.get(candidate.url,{headers:{'User-Agent':UA,Referer:candidate.source_page,Accept:'image/avif,image/webp,image/png,image/jpeg,*/*;q=0.2'},timeout:60000,failOnStatusCode:false})}catch(error){return{...candidate,download_error:error.message}}if(!response.ok())return{...candidate,download_error:`HTTP ${response.status()}`};const bytes=Buffer.from(await response.body()),mime=signatureMime(bytes);if(bytes.length<10000||mime==='unknown')return{...candidate,download_error:`unusable bytes ${bytes.length} ${mime}`};const relative=`candidates/${String(index).padStart(2,'0')}-${slug(candidate.source_key)}-${slug(candidate.provider)}.${extensionFor(mime)}`,path=join(OUT,relative);await mkdir(dirname(path),{recursive:true});await writeFile(path,bytes);let dimensions={width:0,height:0};try{dimensions=identify(path)}catch{}if(dimensions.width<350||dimensions.height<300)return{...candidate,local:relative,mime,bytes:bytes.length,sha256:sha(bytes),...dimensions,download_error:'image below 350x300 floor'};return{...candidate,local:relative,mime,bytes:bytes.length,sha256:sha(bytes),...dimensions,resolved_url:response.url()||candidate.url}}

const control=await readJson(CONTROL);assert(control.version===1&&control.lane==='card-backfill'&&control.record_id==='UC-054','UC-054 control drift');assert(control.actor==='Michael Keaton'&&control.character==='Betelgeuse'&&control.side==='still','UC-054 identity boundary drift');assert(Array.isArray(control.sources)&&control.sources.length===9,'UC-054 discovery source set drift');await mkdir(OUT,{recursive:true});const repository=await repositoryHashes();assert(repository.size===control.expected_repository_hash_count,`repository hash denominator drift ${repository.size}`);const browser=await chromium.launch({headless:true});const context=await browser.newContext({userAgent:UA,viewport:{width:1440,height:1100},locale:'en-US'});const evidence={},raw=[],seen=new Set();try{for(const source of control.sources){const page=await inspectPage(context,source);evidence[source.key]=page;if(page.status!=='loaded'||page.required_terms_missing?.length)continue;if(source.type==='imdb-media'){const expectedPath=new URL(source.source_page).pathname;if(!expectedPath.includes(source.media_id))continue;for(const image of page.images||[]){if(image.kind!=='metadata')continue;addCandidate(raw,seen,{provider:source.provider,source_key:source.key,source_type:source.type,label:`Beetlejuice — IMDb ${source.media_id}`,source_page:source.source_page,url:image.url,source_score:140,source_note:`Exact IMDb media ID ${source.media_id}; page metadata identifies Michael Keaton in Beetlejuice. Visual review must establish that the image depicts the original 1988 Betelgeuse.`})}}else{for(const image of page.images||[]){const score=scoreImage(image,source);if(score<8)continue;addCandidate(raw,seen,{provider:source.provider,source_key:source.key,source_type:source.type,label:image.alt||`${source.provider} page image`,source_page:source.source_page,url:image.url,source_score:score,source_note:'Image exposed by an exact Michael Keaton / Beetlejuice transformation article. Visual review must reject unmasked Keaton, other cast, sequel-only imagery, animation, posters, merchandise, logos and namesakes.',page_context:image.context})}}}raw.sort((a,b)=>(b.source_score||0)-(a.source_score||0)||a.source_key.localeCompare(b.source_key)||a.url.localeCompare(b.url));const selected=raw.slice(0,Number(control.max_candidates||32));const downloaded=[],byteHashes=new Set();for(const candidate of selected){const row=await download(context,candidate,downloaded.length+1);if(row.sha256&&byteHashes.has(row.sha256))continue;if(row.sha256)byteHashes.add(row.sha256);if(row.sha256)row.repository_matches=repository.get(row.sha256)||[];downloaded.push(row)}const usable=downloaded.filter(row=>row.local&&!row.download_error);assert(usable.length>=3,`UC-054 orbit produced only ${usable.length} usable candidates`);const thumbs=[];let number=0;for(const row of usable){const path=join(OUT,'thumbs',`${String(++number).padStart(2,'0')}.jpg`);await mkdir(dirname(path),{recursive:true});magick(join(OUT,row.local),'-auto-orient','-thumbnail','360x300>','-background','#171512','-gravity','center','-extent','360x300','-fill','white','-undercolor','#171512cc','-gravity','south','-pointsize','14','-annotate','+0+4',`${String(number).padStart(2,'0')} ${row.provider} ${row.source_key}`,'-strip','-quality','88',path);thumbs.push(path)}const contact=join(OUT,'contact-sheet.jpg');execFileSync('montage',[...thumbs,'-tile','4x','-geometry','360x300+10+10','-background','#e8e3d9',contact],{stdio:'inherit'});const manifest={version:1,lane:control.lane,record_id:control.record_id,actor:control.actor,character:control.character,production:control.production,year:control.year,side:control.side,expected_subject:control.expected_subject,generated_at:new Date().toISOString(),control_sha256:sha(await readFile(CONTROL)),repository_hash_count:repository.size,page_evidence:evidence,candidates:downloaded,usable_candidate_count:usable.length,contact_sheet:{path:'contact-sheet.jpg',...identify(contact)},disposition:'candidate-only-pending-visual-selection',canonical_mutation:false};await writeJson(join(OUT,'manifest.json'),manifest);await writeJson(join(OUT,'summary.json'),{record_id:'UC-054',actor:'Michael Keaton',character:'Betelgeuse',usable_candidate_count:usable.length,providers:[...new Set(usable.map(row=>row.provider))],candidate_paths:usable.map(row=>({local:row.local,provider:row.provider,source_key:row.source_key,sha256:row.sha256,width:row.width,height:row.height,repository_matches:row.repository_matches||[]}))});const cards=downloaded.map((row,index)=>`<article>${row.local&&!row.download_error?`<img src="${esc(row.local)}" alt="">`:`<div class="bad">${esc(row.download_error||'no image')}</div>`}<h2>${index+1} · ${esc(row.provider)} · ${esc(row.source_key)}</h2><p>${esc(row.label)}</p><p>score ${esc(row.source_score||0)} · ${row.repository_matches?.length?`duplicate: ${esc(row.repository_matches.join(', '))}`:'no exact repository duplicate'}</p><p>${esc(row.source_note)}</p><a href="${esc(row.source_page)}">source page</a><code>${esc(row.sha256||'')}</code></article>`).join('');await writeFile(join(OUT,'review.html'),`<!doctype html><meta charset="utf-8"><style>body{font:14px system-ui;background:#e8e3d9;margin:24px}.sheet{max-width:100%}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(260px,1fr));gap:12px}article{background:white;padding:10px}article img,.bad{width:100%;height:320px;object-fit:contain;background:#171512;color:white}.bad{display:grid;place-items:center}code{display:block;font-size:9px;word-break:break-all}</style><h1>UC-054 · Michael Keaton Betelgeuse still orbit</h1><p>Candidate-only. Approve only the original 1988 Betelgeuse; reject unmasked Keaton, other cast, sequel-only imagery, animation, merchandise, posters, logos and namesakes.</p><img class="sheet" src="contact-sheet.jpg"><div class="grid">${cards}</div>`);console.log(`UC-054 discovery: ${usable.length} usable candidate(s) from ${[...new Set(usable.map(row=>row.provider))].join(', ')}`);console.log(`contact ${contact}`);console.log(`artifact ${OUT}`)}finally{await browser.close()}
