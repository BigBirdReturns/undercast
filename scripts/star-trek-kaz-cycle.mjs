#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
const RECEIPT="data/review/adapter-sdk/star-trek-kaz-cycle.json", CHECKER="scripts/star-trek-kaz-cycle.mjs";
const TASK="ap_e8205bc4b6cf541471be3395", LEASE="lease_a01cea591ec9f448dfda504c", WALL="UC-1366", CYCLE="cycle_690b6e1f79aa674305e73b8a", EVENT="waterline_f901331619e471fed5abfe91", REVIEW="2026-08-13T21:37:01.573Z";
const PERFORMER="James Doohan", ROLE="Kaz", SOURCE="https://memory-alpha.fandom.com/wiki/Kaz", FINGERPRINT="876800402f254f2de8d76438859a30d91bce240143b741de7103c8e2cec957eb";
const MAIN="aac8f5cbe3a230c26098323729f23643e2495573", CANDIDATE="9643fa6146efb2c68fa3a9cc52cf27473e53e08f", TREE="323ccf4de3b516c0ab55f2cd75dc47bbc0647c43", PATH_SHA="857802567c1ba0b62bef986079981ffd48f1313c3ea025083faf30d324cf1bad", REVIEW_ID="f13976ea13f479102dfd6acad036956b0bc7d33c6ea207ab8fd8a7ef5b49bb0d", REVIEW_FILE="79c0162572416c167dae5cacea640eb8d85dd8288ba5930eabff5eb29efe2551", PUBLICATION_RUN=31746273211, PUBLICATION_JOB=94601465440;
const STILL_PATH="images/uc-1366-still.webp", STILL_SHA="1f1a13b7d1d16520edeab1bd58f765c9cc6ff44bf718112699529f7ff8d76617", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/f/fd/Kaz.jpg/revision/latest?cb=20190718033732&path-prefix=en", PORTRAIT_PATH="images/uc-1366-portrait.jpg", PORTRAIT_SHA="bafe5b0824421609fa1e239c7b246b35cfcbe75f225d785b6d78a38633312187", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:James_Doohan_Actor.jpg";
const TOTAL=2228, RESOLVED_FLOOR=397;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-KAZ'&&receipt.canonical_parent===MAIN,'Kaz receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Kaz checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Kaz task drifted');
same(task.performance_modes,['physical-and-voice'],'Kaz queued mode hint drifted');
same(task.wall_ids,[WALL],'Kaz wall binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='The Time Trap'&&card.universe==='Star Trek'&&card.years==='1973'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Kaz canonical record drifted');
ok(card.reveal.includes('Kor')&&card.reveal.includes('Xerius')&&card.reveal.includes('role-specific voice contribution')&&card.reveal.includes('physical performance'),'Kaz performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.source.includes('The_Time_Trap_(episode)')),'Kaz source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Kaz media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY 2.0','Kaz media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Kaz source still drifted');
same(source.portrait,card.portrait,'Kaz source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Kaz facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Kaz still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Kaz portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='physical-and-voice'&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Kaz adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Kaz receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Kaz claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Kaz candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const kaz=byLease.get(LEASE);
ok(kaz?.id===CYCLE&&kaz.outcome==='completed'&&kaz.task_statuses?.[TASK]==='resolved'&&kaz.reviewed_at===REVIEW,'Kaz cycle receipt drifted');
const cb=structuredClone(kaz);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Kaz cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Kaz waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Kaz waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1830,resolved:397,blocked:0,rejected:1,in_flight:0},'Kaz terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const pc=receipt.prior_custody;
ok(sha(fs.readFileSync(pc.guardian_receipt_path))===pc.guardian_receipt_file_sha256&&read(pc.guardian_receipt_path).receipt_sha256===pc.guardian_receipt_identity&&sha(fs.readFileSync(pc.guardian_checker_path))===pc.guardian_checker_sha256,'Kaz lost Guardian custody');
ok(sha(fs.readFileSync(pc.landru_receipt_path))===pc.landru_receipt_file_sha256&&read(pc.landru_receipt_path).receipt_sha256===pc.landru_receipt_identity&&sha(fs.readFileSync(pc.landru_checker_path))===pc.landru_checker_sha256,'Kaz lost Landru custody');
ok(sha(fs.readFileSync(pc.curzon_receipt_path))===pc.curzon_receipt_file_sha256&&read(pc.curzon_receipt_path).receipt_sha256===pc.curzon_receipt_identity&&sha(fs.readFileSync(pc.curzon_checker_path))===pc.curzon_checker_sha256,'Kaz lost Curzon custody');
ok(sha(fs.readFileSync(pc.armus_receipt_path))===pc.armus_receipt_file_sha256&&read(pc.armus_receipt_path).receipt_sha256===pc.armus_receipt_identity&&sha(fs.readFileSync(pc.armus_checker_path))===pc.armus_checker_sha256&&sha(fs.readFileSync(pc.armus_historical_checker_path))===pc.armus_historical_checker_sha256,'Kaz lost Armus custody');
ok(sha(fs.readFileSync(pc.m5_receipt_path))===pc.m5_receipt_file_sha256&&read(pc.m5_receipt_path).receipt_sha256===pc.m5_receipt_identity&&sha(fs.readFileSync(pc.m5_checker_path))===pc.m5_checker_sha256&&sha(fs.readFileSync(pc.m5_previous_wrapper_path))===pc.m5_previous_wrapper_sha256&&sha(fs.readFileSync(pc.m5_historical_checker_path))===pc.m5_historical_checker_sha256&&sha(fs.readFileSync(pc.m5_composability_receipt_path))===pc.m5_composability_receipt_file_sha256&&read(pc.m5_composability_receipt_path).receipt_sha256===pc.m5_composability_receipt_identity,'Kaz lost M-5 custody');
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Kaz qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Kaz execution custody drifted');
ok(read('package.json').scripts?.['star-trek:kaz-cycle:check']==='node scripts/star-trek-kaz-cycle.mjs','Kaz checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.other_doohan_roles_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.role_specific_maker_attributed===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false&&receipt.boundary?.accidental_canonical_carrier_removed===true,'Kaz authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1366/'),'Kaz permanent route missing');
ok(!fs.existsSync('.github/workflows/star-trek-kaz-cycle-terminal.yml'),'Kaz carrier workflow survived canonical publication');
console.log('star-trek-kaz-cycle: PASS — exact animated Kaz voice custody, separate exact character and licensed performer media, separate Kor and Xerius role identity, and no physical-performance transfer, reviewed waterline closure, immutable prior-cycle custody, and later-cycle bounds are intact');
