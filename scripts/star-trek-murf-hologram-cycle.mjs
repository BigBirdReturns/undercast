#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-murf-hologram-cycle.json", CHECKER="scripts/star-trek-murf-hologram-cycle.mjs";
const TASK="ap_49dd2fbd7c27aaa730a3d114", LEASE="lease_1d0936d1443d8c22d0bafcb4", WALL="UC-1374", CYCLE="cycle_4c29edc7c5cf36ab083513c7", EVENT="waterline_c7e390a30084ffed512b539b", REVIEW="2026-08-15T17:14:42.233Z";
const PERFORMER="Dee Bradley Baker", ROLE="Murf (hologram)", SOURCE="https://memory-alpha.fandom.com/wiki/Murf_(hologram)", IMPOSTER_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Imposter_Syndrome_(episode)", FAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/The_Fast_and_the_Curious_(episode)", BEAUTY_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Is_There_in_Beauty_No_Truth%3F_(episode)", FINGERPRINT="8375cff78f23688e3ddef09cb2adfc8eef7750cfee270017ddc0c473efbf687d";
const MAIN="6b8c7ea9fb8a4f076ea483a980923485756bf490", CANDIDATE="aa82e5251be28bea00339750cb90d8a6210fb488", TREE="b530a3beec74dcb34dca2ac6f36a2718c48ad05b", PATH_SHA="301f6c50f3a8f9e06433b97af7030953f40cb4ffa9e8778d6f34e999ec4deac2", REVIEW_ID="c36e0eabe06466a63416b006d071e089d28f4eb817824ea956ad11bc19086c6d", REVIEW_FILE="ff48a0e4bc279f88f82f87641ca603e7882326da4a9c75d0ed62700f5a17de77", PUBLICATION_RUN=31897695715, PUBLICATION_JOB=95043459740;
const STILL_ABSENCE_SHA="e8832a46262da7fe562589786dec376dcc1db3ea8ccd3032c1e39384f267f1f8", PORTRAIT_PATH="images/uc-1374-portrait.jpg", PORTRAIT_SHA="3fbb0b157b36bb7248adf3e8385c9939cce8c35a0f87ce9bd0846a72e1d994ca", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Dee_Bradley_Baker_by_Gage_Skidmore_3.jpg";
const PRIOR_RECEIPT="data/review/adapter-sdk/star-trek-murf-cycle.json", PRIOR_RECEIPT_FILE="80683fe216315331d783203bfc183263bbdf3c9e202e205b5f13945d71e4b787", PRIOR_RECEIPT_ID="018cfb4a40e6c8a53d550c92ff7143dd2c036b36b4e26d9495b5f9842f96373a", PRIOR_CHECKER="scripts/star-trek-murf-cycle.mjs", PRIOR_CHECKER_SHA="7c05cd92edf709dcb56cb09324b73b00ce88c2fd766b98714ac68c226c76a36c", PRIOR_CYCLE="cycle_40248cbf73afbe1269a19bd7";
const TOTAL=2228, RESOLVED_FLOOR=405;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-MURF-HOLOGRAM'&&receipt.canonical_parent===MAIN,'Murf hologram receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Murf hologram checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Murf hologram task drifted');
same(task.performance_modes,['voice-animation'],'Murf hologram queued mode hint drifted');
same(task.wall_ids,[WALL],'Murf hologram wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Murf hologram task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='Star Trek: Prodigy'&&card.universe==='Star Trek'&&card.years==='2024'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Murf hologram canonical record drifted');
ok(card.reveal.includes('voice did not seem altered')&&card.reveal.includes('Murf-esque vocalizations')&&card.reveal.includes('voice-only performance claim')&&card.reveal.includes('Ordinary Murf')&&card.reveal.includes('Gwyndala hologram')&&card.reveal.includes('honest absence')&&card.reveal.includes('diegetic narrative information')&&card.reveal.includes('No physical performance')&&card.reveal.includes('production-shop'),'Murf hologram performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.source===IMPOSTER_EPISODE_SOURCE)&&card.references?.some(x=>x.source===FAST_EPISODE_SOURCE)&&card.references?.some(x=>x.source===BEAUTY_EPISODE_SOURCE),'Murf hologram source custody drifted');
ok(card.still===undefined&&!fs.existsSync('images/uc-1374-still.webp')&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Murf hologram media bytes drifted');
ok(card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 3.0','Murf hologram portrait provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
ok(source?.still===null,'Murf hologram source still absence drifted');
same(source.portrait,card.portrait,'Murf hologram source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(facets.length===2&&still?.status==='absent'&&still.asset===null&&still.risk_codes?.includes('source-declared-absent')&&portrait?.status==='verified'&&portrait.asset?.sha256===PORTRAIT_SHA,'Murf hologram facets drifted');
ok(portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Murf hologram portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.performance_scope==='unaltered Murf voice and Murf-esque vocalizations supplied for the holographic duplicate'&&receipt.task?.voice_alteration==='source says the voice did not seem to be altered'&&receipt.task?.ordinary_murf_role==='separate canonical performance'&&receipt.task?.gwyndala_hologram_role==='separate canonical performance'&&receipt.task?.diegetic_creators_are_not_real_world_maker_credits===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Murf hologram adjudication drifted');
ok(receipt.media?.still==='absent'&&receipt.media?.still_absence_receipt_sha256===STILL_ABSENCE_SHA,'Murf hologram absence receipt drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Murf hologram receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Murf hologram claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Murf hologram candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const murf=byLease.get(LEASE);
ok(murf?.id===CYCLE&&murf.outcome==='completed'&&murf.task_statuses?.[TASK]==='resolved'&&murf.reviewed_at===REVIEW,'Murf hologram cycle receipt drifted');
const cb=structuredClone(murf);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Murf hologram cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Murf hologram waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Murf hologram waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1822,resolved:405,blocked:0,rejected:1,in_flight:0},'Murf hologram terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(PRIOR_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(PRIOR_RECEIPT))===PRIOR_RECEIPT_FILE&&prior.receipt_sha256===PRIOR_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===PRIOR_CYCLE&&sha(fs.readFileSync(PRIOR_CHECKER))===PRIOR_CHECKER_SHA,'Murf hologram lost prior Murf custody');
const priorRun=spawnSync(process.execPath,[PRIOR_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Murf checker failed under Murf hologram custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Murf hologram qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Murf hologram execution custody drifted');
ok(read('package.json').scripts?.['star-trek:murf-hologram-cycle:check']==='node scripts/star-trek-murf-hologram-cycle.mjs','Murf hologram checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.ordinary_murf_role_conflated===false&&receipt.boundary?.gwyndala_hologram_role_conflated===false&&receipt.boundary?.diegetic_creators_promoted_to_maker_credit===false&&receipt.boundary?.honest_still_absence===true&&receipt.boundary?.ordinary_murf_image_substituted===false&&receipt.boundary?.generic_animation_substituted===false&&receipt.boundary?.performer_portrait_substituted_as_character===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Murf hologram authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1374/'),'Murf hologram permanent route missing');
console.log('star-trek-murf-hologram-cycle: PASS — exact unaltered hologram voice custody, honest character-still absence, separate licensed performer portrait, ordinary Murf and Gwyndala-hologram separation, no fictional-to-real maker promotion, reviewed waterline closure, immutable Murf custody, and later-cycle bounds are intact');
