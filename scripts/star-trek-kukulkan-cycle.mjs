#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-kukulkan-cycle.json", CHECKER="scripts/star-trek-kukulkan-cycle.mjs";
const TASK="ap_8aa8780eda59987cb5a1de36", LEASE="lease_46b3c15264eece5fbd3e39ed", WALL="UC-1390", CYCLE="cycle_0175bc5e1dd368374c066342", EVENT="waterline_2fd3e11005b4a2f95f74de87", REVIEW="2026-08-20T01:17:39.774Z";
const PERFORMER="James Doohan", ROLE="Kukulkan", SOURCE="https://memory-alpha.fandom.com/wiki/Kukulkan", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/How_Sharper_Than_a_Serpent%27s_Tooth_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/How_Sharper_Than_a_Serpent%27s_Tooth_(episode)", FINGERPRINT="77f5acaf275c3880b0f0139d0726789eefeff1cf7de1710f00e9d1bca4427b9c";
const MAIN="af8c0891b38275889bc90ca76af763ce6dd9b59c", CANDIDATE="2453707296e9848ee60e5c22ad336354453a97f3", TREE="32be3ba8866cf48b6302d345fe2ba07696ff29e0", PATH_SHA="7433cc3f43aaf5080b2a0ff8ff756ab40e45e39573559b2b6b6714f8bb2f5361", REVIEW_ID="8e8ab4510cfb20b9d9e141692cdff173332a31568d93e556e62f265d9c77063a", REVIEW_FILE="cfb2533e59855ba7482493e07b330dc72357d16a9f821c774af45a4d6f3abf27", PUBLICATION_RUN=32320408106, PUBLICATION_JOB=96281315695;
const STILL_PATH="images/uc-1390-still.webp", STILL_SHA="1b5ea8e3828008fa85be15aea04fb94d43ba8630f286ec3567e0fb47d419ecfc", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/c/c2/Kukulkan.jpg/revision/latest?cb=20061124010215&path-prefix=en", PORTRAIT_PATH="images/uc-1390-portrait.jpg", PORTRAIT_SHA="0f77a999b33ee7bd9aeb1a22ab4eb29283bfb185c15f76b4712352606a49e94c", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:James_Doohan_-_Walk_of_Fame_-_July_21_2005.jpg";
const PRIOR_ARI_RECEIPT="data/review/adapter-sdk/star-trek-kol-tai-cycle.json", PRIOR_ARI_RECEIPT_FILE="ed2deefff465b105ed82389da6477b1a1152b9d75a0c42d9e908fbf6ba0eaa40", PRIOR_ARI_RECEIPT_ID="5babd08015b5329962ba923a6d6ccaf12e3c4a4fa03c919c60e5b25cc469341e", PRIOR_ARI_CHECKER="scripts/star-trek-kol-tai-cycle.mjs", PRIOR_ARI_CHECKER_SHA="6931341bfd32eadd64ed5bef6bcccb2cc8ca87ce2a46edd09911d90e002b6638", PRIOR_ARI_CYCLE="cycle_a2b0baa44a5fbbdc9c2ddb6b";
const TOTAL=2228, RESOLVED_FLOOR=422;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-KUKULKAN'&&receipt.canonical_parent===MAIN,'Kukulkan receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Kukulkan checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Kukulkan task drifted');
same(task.performance_modes,['voice-animation'],'Kukulkan queued mode hint drifted');
same(task.wall_ids,[WALL],'Kukulkan wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Kukulkan task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==="Star Trek: The Animated Series (How Sharper Than a Serpent's Tooth)"&&card.universe==='Star Trek'&&card.years==='1974'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Kukulkan canonical record drifted');
ok(card.reveal.includes('identifies James Doohan in Kukulkan’s voice-performer field')&&card.reveal.includes('voice-only performance claim for Kukulkan')&&card.reveal.includes('Kukulkan')&&card.reveal.includes('Cadmar')&&card.reveal.includes('Cheeron')&&card.reveal.includes('Ari bn Bem')&&card.reveal.includes('role-specific still')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing')&&card.reveal.includes('production-shop'),'Kukulkan performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'Kukulkan source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Kukulkan media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 3.0','Kukulkan media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Kukulkan source still drifted');
same(source.portrait,card.portrait,'Kukulkan source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Kukulkan facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Kukulkan still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Kukulkan portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Unknown'&&receipt.task?.episode==="How Sharper Than a Serpent's Tooth"&&receipt.task?.performance_scope==="James Doohan's voice performance as Kukulkan in How Sharper Than a Serpent's Tooth (1974)"&&receipt.task?.kol_tai_role_not_conflated===true&&receipt.task?.karl_four_role_not_conflated===true&&receipt.task?.domar_role_not_conflated===true&&receipt.task?.chuft_captain_role_not_conflated===true&&receipt.task?.role_still_is_character_evidence_only===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Kukulkan adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Kukulkan receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Kukulkan claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Kukulkan candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const unitkukulkan=byLease.get(LEASE);
ok(unitkukulkan?.id===CYCLE&&unitkukulkan.outcome==='completed'&&unitkukulkan.task_statuses?.[TASK]==='resolved'&&unitkukulkan.reviewed_at===REVIEW,'Kukulkan cycle receipt drifted');
const cb=structuredClone(unitkukulkan);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Kukulkan cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Kukulkan waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Kukulkan waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1805,resolved:422,blocked:0,rejected:1,in_flight:0},'Kukulkan terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(PRIOR_ARI_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(PRIOR_ARI_RECEIPT))===PRIOR_ARI_RECEIPT_FILE&&prior.receipt_sha256===PRIOR_ARI_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===PRIOR_ARI_CYCLE&&sha(fs.readFileSync(PRIOR_ARI_CHECKER))===PRIOR_ARI_CHECKER_SHA,'Kukulkan lost prior Kol-Tai custody');
const priorRun=spawnSync(process.execPath,[PRIOR_ARI_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Kukulkan checker failed under Kol-Tai custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Kukulkan qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Kukulkan execution custody drifted');
ok(read('package.json').scripts?.['star-trek:kukulkan-cycle:check']==='node scripts/star-trek-kukulkan-cycle.mjs','Kukulkan checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.voice_credit_promoted_to_processing_credit===false&&receipt.boundary?.separate_role_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Kukulkan authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1390/'),'Kukulkan permanent route missing');
console.log('star-trek-kukulkan-cycle: PASS — exact Kukulkan voice custody, exact independent character and performer media, no physical, animation, processing, or maker promotion, separate Kol-Tai, Karl Four, Domar, Chuft-Captain, Cadmar, Cheeron, and Ari bn Bem custody, reviewed waterline closure, immutable Kol-Tai predecessor custody, and later-cycle bounds are intact');
