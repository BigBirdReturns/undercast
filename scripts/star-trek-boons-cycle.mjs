#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-boons-cycle.json", CHECKER="scripts/star-trek-boons-cycle.mjs";
const TASK="ap_f6a58746fd319324a157f606", LEASE="lease_2ef9dc05dfc4b349e68f8c4c", WALL="UC-1377", CYCLE="cycle_7f2c05bc7641a8bd68aeab9a", EVENT="waterline_4b15c1b9a099376abcb162cc", REVIEW="2026-08-15T21:13:45.572Z";
const PERFORMER="Fred Tatasciore", ROLE="Boons", SOURCE="https://memory-alpha.fandom.com/wiki/Boons", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/All_the_World's_a_Stage_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/All_the_World's_a_Stage_(episode)", FINGERPRINT="d73cfb67526c41df098b61b3c8239878ac88c1a8187ebf3e14990697ca9ed121";
const MAIN="c914280ef940b68567007fa1c0e6d9c65a9e91a9", CANDIDATE="c96f19c6f17a8d3561f3af8cbee7d13734987a91", TREE="47b5d6f8c4ca516b248e0de6535e03d266b8e0e4", PATH_SHA="93333aeb6803f1b1ec556487643b3083b8ab6c2ee3678bc6ea8e64020782b809", REVIEW_ID="c38a3ebfb08706e06952b05da2c7cd94c8a00362ddc1ad3298961fdc9d4e5b8c", REVIEW_FILE="27a2fb3501af1adb499406ed363d025a704f0c945dad7003207c46e8d65969e9", PUBLICATION_RUN=31908802328, PUBLICATION_JOB=95070645687;
const STILL_PATH="images/uc-1377-still.webp", STILL_SHA="293a3a23b0fb6754aa965e4eb88341bead104353258f73ac06ade4d4101aca11", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/e/e5/Boons.jpg/revision/latest?cb=20221110175115&path-prefix=en", PORTRAIT_PATH="images/uc-1377-portrait.jpg", PORTRAIT_SHA="85b17097b108155a6a9e70863f858d7ae93f8a97d93ca2b04b3b2f53a49d97ad", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Fred_Tatasciore_(27230252708).jpg";
const PRIOR_BRT_TENDI_RECEIPT="data/review/adapter-sdk/star-trek-brt-tendi-cycle.json", PRIOR_BRT_TENDI_RECEIPT_FILE="79c306ff3184f79b3d62e547eb5738e30ad7f9d4dc1d831d908a00c6fe358e8d", PRIOR_BRT_TENDI_RECEIPT_ID="34e9f84263f3bb1dc4c8d00a0caa99db1ddc9a9f4626c039b3ca6a939a7f4d67", PRIOR_BRT_TENDI_CHECKER="scripts/star-trek-brt-tendi-cycle.mjs", PRIOR_BRT_TENDI_CHECKER_SHA="f191e367a7d45cd5f356f73906e252794f17cd4d7e81f4dd930c05735de1f8c5", PRIOR_BRT_TENDI_CYCLE="cycle_1157bf795d05883bf9b956ed";
const TOTAL=2228, RESOLVED_FLOOR=408;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-BOONS'&&receipt.canonical_parent===MAIN,'Boons receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Boons checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Boons task drifted');
same(task.performance_modes,['voice-animation'],'Boons queued mode hint drifted');
same(task.wall_ids,[WALL],'Boons wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Boons task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==="Star Trek: Prodigy (All the World's a Stage)"&&card.universe==='Star Trek'&&card.years==='2022'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Boons canonical record drifted');
ok(card.reveal.includes('Fred Tatasciore in the actor field')&&card.reveal.includes('voice-only performance claim for Boons')&&card.reveal.includes('namesake') && card.reveal.includes('Sprok')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing')&&card.reveal.includes('production-shop'),'Boons performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'Boons source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Boons media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 2.0','Boons media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Boons source still drifted');
same(source.portrait,card.portrait,'Boons source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Boons facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Boons still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Boons portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Enderprizian'&&receipt.task?.episode==="All the World's a Stage"&&receipt.task?.performance_scope==="Fred Tatasciore's voice performance as Boons in All the World's a Stage (2022)"&&receipt.task?.leonard_mccoy_namesake_not_conflated===true&&receipt.task?.sprok_role_not_conflated===true&&receipt.task?.james_t_role_not_conflated===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Boons adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Boons receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Boons claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Boons candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const unitboons=byLease.get(LEASE);
ok(unitboons?.id===CYCLE&&unitboons.outcome==='completed'&&unitboons.task_statuses?.[TASK]==='resolved'&&unitboons.reviewed_at===REVIEW,'Boons cycle receipt drifted');
const cb=structuredClone(unitboons);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Boons cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Boons waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Boons waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1819,resolved:408,blocked:0,rejected:1,in_flight:0},'Boons terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(PRIOR_BRT_TENDI_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(PRIOR_BRT_TENDI_RECEIPT))===PRIOR_BRT_TENDI_RECEIPT_FILE&&prior.receipt_sha256===PRIOR_BRT_TENDI_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===PRIOR_BRT_TENDI_CYCLE&&sha(fs.readFileSync(PRIOR_BRT_TENDI_CHECKER))===PRIOR_BRT_TENDI_CHECKER_SHA,'Boons lost prior B’Rt Tendi custody');
const priorRun=spawnSync(process.execPath,[PRIOR_BRT_TENDI_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'B’Rt Tendi checker failed under Boons custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Boons qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Boons execution custody drifted');
ok(read('package.json').scripts?.['star-trek:boons-cycle:check']==='node scripts/star-trek-boons-cycle.mjs','Boons checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.voice_credit_promoted_to_processing_credit===false&&receipt.boundary?.separate_role_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Boons authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1377/'),'Boons permanent route missing');
console.log('star-trek-boons-cycle: PASS — exact Boons voice custody, exact independent character and performer media, no physical, animation, processing, or maker promotion, separate namesake and Enderprizian-role custody, reviewed waterline closure, immutable B’Rt Tendi custody, and later-cycle bounds are intact');
