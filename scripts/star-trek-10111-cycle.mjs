#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-10111-cycle.json", CHECKER="scripts/star-trek-10111-cycle.mjs";
const TASK="ap_fc053f0be18a1617be30d4ea", LEASE="lease_17548d693dccdb28b2034070", WALL="UC-1375", CYCLE="cycle_961225a8852605b7acbda032", EVENT="waterline_f7d33418ef2f018d2dca0454", REVIEW="2026-08-15T19:23:59.622Z";
const PERFORMER="Fred Tatasciore", ROLE="10111", SOURCE="https://memory-alpha.fandom.com/wiki/10111", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Where_Pleasant_Fountains_Lie_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/A_Mathematically_Perfect_Redemption_(episode)", FINGERPRINT="ea790a930b3044bccbe117caff125a8fd644911b45aba4d0e6e6b5c38d7e5b5e";
const MAIN="2a336e40098814a101d5adeb341e33aba117802a", CANDIDATE="616be7cc3bf0630abe4f3cd487eb7c08d25ca78c", TREE="4307fb6ada864f9760dd173e22295f49b9b85204", PATH_SHA="e191cd52c8eb3592472af47f18f3454ba2a82317e255ae3cac3872184bfe49dd", REVIEW_ID="af91b9359ceacb07445fb732fe39814e2d75c079726310bc51b418cc28f1ea72", REVIEW_FILE="c42598100bdda94274a135394791fb860d2d5c3fd6da36c64197c32619bafe86", PUBLICATION_RUN=31903748441, PUBLICATION_JOB=95058334181;
const STILL_PATH="images/uc-1375-still.webp", STILL_SHA="8b000b445b52e1fab84781931637e397fb7068f8d4fa766f0c3faf1c70e28580", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/6/66/10111.jpg/revision/latest?cb=20210924175403&path-prefix=en", PORTRAIT_PATH="images/uc-1375-portrait.jpg", PORTRAIT_SHA="0c23d1e29884c52b3d1097d5a9c0c3e9cfefa4c302a93fb474eafd9a2d498209", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Fred_Tatasciore_by_Gage_Skidmore.jpg";
const MH_RECEIPT="data/review/adapter-sdk/star-trek-murf-hologram-cycle.json", MH_RECEIPT_FILE="2d5bcc98a31c0885932dc389f56861ac58c9e334d9bac9cf288e4e630ee09951", MH_RECEIPT_ID="ed9946da38e92cbdf0dbf2d24c89151dde268e9a5d53a951df22c51c435795d3", MH_CHECKER="scripts/star-trek-murf-hologram-cycle.mjs", MH_CHECKER_SHA="a6d03178e375c1a1cae52fd510708e00a5b98a2cd5a9ef6fc74b7ef6cc77b865", MH_CYCLE="cycle_4c29edc7c5cf36ab083513c7";
const TOTAL=2228, RESOLVED_FLOOR=406;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-10111'&&receipt.canonical_parent===MAIN,'10111 receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'10111 checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'10111 task drifted');
same(task.performance_modes,['voice-animation'],'10111 queued mode hint drifted');
same(task.wall_ids,[WALL],'10111 wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'10111 task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='Star Trek: Lower Decks (Where Pleasant Fountains Lie)'&&card.universe==='Star Trek'&&card.years==='2021'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'10111 canonical record drifted');
ok(card.reveal.includes('Fred Tatasciore as the voice of 10111 in “Where Pleasant Fountains Lie”')&&card.reveal.includes('voice-only performance claim limited to the 2021 first appearance')&&card.reveal.includes('unknown actor')&&card.reveal.includes('Tyrannikillicus remains a separate character')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('sound processing')&&card.reveal.includes('production-shop'),'10111 performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'10111 source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'10111 media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 3.0','10111 media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'10111 source still drifted');
same(source.portrait,card.portrait,'10111 source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'10111 facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','10111 still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','10111 portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='sentient computer'&&receipt.task?.first_appearance==='Where Pleasant Fountains Lie'&&receipt.task?.later_appearance==='A Mathematically Perfect Redemption'&&receipt.task?.later_appearance_performer==='unknown actor'&&receipt.task?.performance_scope==='voice performance limited to Where Pleasant Fountains Lie (2021)'&&receipt.task?.source_wording==='The frozen source credits Fred Tatasciore as 10111’s voice in “Where Pleasant Fountains Lie” and an unknown actor in “A Mathematically Perfect Redemption.”'&&receipt.task?.first_appearance_scope_enforced===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'10111 adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'10111 receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'10111 claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'10111 candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const unit10111=byLease.get(LEASE);
ok(unit10111?.id===CYCLE&&unit10111.outcome==='completed'&&unit10111.task_statuses?.[TASK]==='resolved'&&unit10111.reviewed_at===REVIEW,'10111 cycle receipt drifted');
const cb=structuredClone(unit10111);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'10111 cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'10111 waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'10111 waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1821,resolved:406,blocked:0,rejected:1,in_flight:0},'10111 terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(MH_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(MH_RECEIPT))===MH_RECEIPT_FILE&&prior.receipt_sha256===MH_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===MH_CYCLE&&sha(fs.readFileSync(MH_CHECKER))===MH_CHECKER_SHA,'10111 lost prior Murf-hologram custody');
const priorRun=spawnSync(process.execPath,[MH_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'Murf-hologram checker failed under 10111 custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'10111 qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'10111 execution custody drifted');
ok(read('package.json').scripts?.['star-trek:10111-cycle:check']==='node scripts/star-trek-10111-cycle.mjs','10111 checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.later_actor_inferred===false&&receipt.boundary?.tyrannikillicus_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'10111 authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1375/'),'10111 permanent route missing');
console.log('star-trek-10111-cycle: PASS — exact Where Pleasant Fountains Lie voice custody, separate later unknown-actor boundary, exact character and performer media, no physical, animation, processing, or maker promotion, separate Tyrannikillicus custody, reviewed waterline closure, immutable Murf-hologram custody, and later-cycle bounds are intact');
