#!/usr/bin/env node
import { chromium } from '@playwright/test';
import { createHash } from 'node:crypto';
import { execFileSync } from 'node:child_process';
import { mkdir, readFile, readdir, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';

const CONTROL='.github/CARD-BACKFILL-UC-069.json';
const OUT=process.env.OUT||'/tmp/card-backfill-uc-069';
const UA='Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/132 Safari/537.36 UNDERCAST-card-backfill/1.0';
const sha=value=>createHash('sha256').update(value).digest('hex');
const norm=value=>String(value||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[’‘]/g,"'").replace(/[^a-zA-Z0-9']+/g,' ').trim().toLowerCase();
const slug=value=>norm(value).replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'unknown';
const assert=(condition,message)=>{if(!condition)throw new Error(message)};
const readJson=async path=>JSON.parse(await readFile(path,'utf8'));
const writeJson=async(path,value)=>{await mkdir(dirname(path),{recursive:true});await writeFile(path,JSON.stringify(value,null,2)+'\n')};
function signatureMime(bytes){if(bytes.length>=3&&bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff)return'image/jpeg';if(bytes.length>=8&&bytes.subarray(0,8).equals(Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a])))return'image/png';if(bytes.length>=12&&bytes.subarray(0,4).toString('ascii')==='RIFF'&&bytes.subarray(8,12).toString('ascii')==='WEBP')return'image/webp';return'unknown'}
function extensionFor(mime){return mime==='image/jpeg'?'jpg':mime==='image/png'?'png':mime==='image/webp'?'webp':'bin'}
function identify(path){const text=execFileSync(process.env.MAGICK_CMD||'magick',['identify','-format','%w %h',path],{encoding:'utf8'}).trim();const[width,height]=text.split(/\s+/).map(Number);assert(width>0&&height>0,`cannot identify ${path}`);return{width,height}}
function magick(...args){execFileSync(process.env.MAGICK_CMD||'magick',args,{stdio:'inherit'})}
async function walkImages(root,out=[]){let entries;try{entries=await readdir(root,{withFileTypes:true})}catch{return out}for(const entry of entries){const path=join(root,entry.name);if(entry.isDirectory())await walkImages(path,out);else if(/\.(?:jpe?g|png|webp)$/i.test(entry.name))out.push(path)}return out}
async function repositoryHashes(){const map=new Map();try{const manifest=await readJson('data/media-manifest.json');for(const[path,row]of Object.entries(manifest.assets||{})){if(!/^[0-9a-f]{64}$/i.test(row?.sha256||''))continue;const list=map.get(row.sha256)||[];list.push(`manifest:${path}`);map.set(row.sha256,list)}}catch{}for(const path of await walkImages('images')){try{const hash=sha(await readFile(path));const list=map.get(hash)||[];list.push(`file:${path}`);map.set(hash,list)}catch{}}return map}
function canonicalAssetStem(value){const url=new URL(value);url.search='';return url.href}
function addSeed(map,seed){let url;try{url=new URL(seed.url,control.source.source_page).href}catch{return}if(!/^https:\/\/static\.dc\.com\//.test(url))return;const stem=canonicalAssetStem(url);const prior=map.get(stem)||{key:seed.key||`dc-${map.size+1}`,label:seed.label||'',stem,source_urls:[],contexts:[],origins:[]};if(!prior.source_urls.includes(url))prior.source_urls.push(url);if(seed.context&&!prior.contexts.includes(seed.context))prior.contexts.push(seed.context.slice(0,1600));if(seed.origin&&!prior.origins.includes(seed.origin))prior.origins.push(seed.origin);if(seed.label&&!prior.label)prior.label=seed.label;map.set(stem,prior)}
function variantUrls(seed){const rows=[];const push=(url,kind)=>{if(!rows.some(row=>row.url===url))rows.push({url,kind})};for(const source of seed.source_urls)push(source,'page-delivery');push(seed.stem,'unparameterized');for(const width of control.probe_widths||[])push(`${seed.stem}?w=${width}`,`width-${width}`);return rows}
async function download(context,seed,variant,index){let response;try{response=await context.request.get(variant.url,{headers:{'User-Agent':UA,Referer:control.source.source_page,Accept:'image/jpeg,image/webp,image/png,image/*,*/*;q=0.2'},timeout:60000,failOnStatusCode:false})}catch(error){return{seed_key:seed.key,seed_label:seed.label,asset_stem:seed.stem,probe_url:variant.url,probe_kind:variant.kind,download_error:error.message}}if(!response.ok())return{seed_key:seed.key,seed_label:seed.label,asset_stem:seed.stem,probe_url:variant.url,probe_kind:variant.kind,download_error:`HTTP ${response.status()}`};const bytes=Buffer.from(await response.body()),mime=signatureMime(bytes);if(bytes.length<8000||mime==='unknown')return{seed_key:seed.key,seed_label:seed.label,asset_stem:seed.stem,probe_url:variant.url,probe_kind:variant.kind,download_error:`unusable ${bytes.length} ${mime}`};const local=`candidates/${String(index).padStart(2,'0')}-${slug(seed.key)}-${slug(variant.kind)}.${extensionFor(mime)}`,path=join(OUT,local);await mkdir(dirname(path),{recursive:true});await writeFile(path,bytes);let dimensions={width:0,height:0};try{dimensions=identify(path)}catch{}if(dimensions.width<400||dimensions.height<250)return{seed_key:seed.key,seed_label:seed.label,asset_stem:seed.stem,probe_url:variant.url,probe_kind:variant.kind,local,mime,bytes:bytes.length,sha256:sha(bytes),...dimensions,download_error:'image below 400x250 floor'};return{seed_key:seed.key,seed_label:seed.label,asset_stem:seed.stem,source_urls:seed.source_urls,source_contexts:seed.contexts,source_origins:seed.origins,probe_url:variant.url,probe_kind:variant.kind,resolved_url:response.url()||variant.url,local,mime,bytes:bytes.length,sha256:sha(bytes),...dimensions}}

const control=await readJson(CONTROL);
assert(control.version===1&&control.lane==='card-backfill'&&control.record_id==='UC-069','UC-069 control scope drift');
assert(control.actor==='Danny DeVito'&&control.character==='The Penguin'&&control.side==='still','UC-069 identity boundary drift');
assert(control.selector_artifact?.artifact_id===8636218683&&control.selector_artifact?.head_sha==='336c9408be9b400aacbff4f393c26116518ebc3e','UC-069 selector custody drift');
assert(control.source?.provider==='DC'&&control.source?.pinned_assets?.length===3,'UC-069 official source set drift');
await mkdir(OUT,{recursive:true});
const repository=await repositoryHashes();
assert(repository.size===control.expected_repository_hash_count,`repository hash denominator drift ${repository.size}`);
const browser=await chromium.launch({headless:true});
const context=await browser.newContext({userAgent:UA,viewport:{width:1440,height:1100},locale:'en-US'});
try{
  const page=await context.newPage();
  const navigation=await page.goto(control.source.source_page,{waitUntil:'domcontentloaded',timeout:90000});
  await page.waitForTimeout(1600);
  const body=await page.locator('body').innerText().catch(()=>''),html=await page.content(),title=await page.title(),hay=norm(body+' '+html);
  const missingTerms=control.source.required_terms.filter(term=>!hay.includes(norm(term)));
  assert(missingTerms.length===0,`official DC page lacks ${missingTerms.join(', ')}`);
  const expected=new URL(control.source.source_page),resolved=new URL(page.url());
  assert(expected.hostname===resolved.hostname&&expected.pathname===resolved.pathname,'official DC page resolved away from exact Batman Returns path');
  assert(norm(title).includes('batman returns 1992'),'official DC title drift');
  const screenshot='source-page.png';await page.screenshot({path:join(OUT,screenshot),fullPage:true});
  const pageBytes=await readFile(join(OUT,screenshot));
  const extracted=await page.evaluate(()=>{const absolute=value=>{try{return new URL(value,document.baseURI).href}catch{return''}};const rows=[];for(const selector of['meta[property="og:image"]','meta[property="og:image:secure_url"]','meta[name="twitter:image"]','meta[name="twitter:image:src"]'])for(const meta of document.querySelectorAll(selector))rows.push({url:absolute(meta.content),label:'',context:document.title,origin:'metadata'});for(const image of document.images){const values=[image.currentSrc,image.src,image.dataset.src,image.dataset.lazySrc,image.dataset.original,image.dataset.image].filter(Boolean);for(const part of String(image.srcset||image.dataset.srcset||'').split(',')){const value=part.trim().split(/\s+/)[0];if(value)values.push(value)}const figure=image.closest('figure');const nearby=[image.alt,figure?.querySelector('figcaption')?.textContent,image.parentElement?.textContent?.slice(0,600),document.title].filter(Boolean).join(' ').replace(/\s+/g,' ').slice(0,1600);for(const value of values)rows.push({url:absolute(value),label:image.alt||'',context:nearby,origin:'dom-image'})}for(const element of document.querySelectorAll('*')){const bg=getComputedStyle(element).backgroundImage;for(const match of String(bg||'').matchAll(/url\(["']?([^"')]+)["']?\)/g))rows.push({url:absolute(match[1]),label:'',context:[element.getAttribute('aria-label'),element.textContent?.slice(0,300),document.title].filter(Boolean).join(' '),origin:'background-image'})}return rows});
  const seeds=new Map();
  for(const asset of control.source.pinned_assets)addSeed(seeds,{...asset,origin:'pinned-control',context:asset.label});
  for(const row of extracted){const contextText=norm([row.label,row.context].join(' '));const stem='';try{const url=new URL(row.url);if(url.hostname!=='static.dc.com')continue;const pinnedMatch=control.source.pinned_assets.some(asset=>canonicalAssetStem(asset.url)===canonicalAssetStem(row.url));const exactLabel=contextText.includes('batman returns 1992');if(!pinnedMatch&&!exactLabel)continue;addSeed(seeds,{key:pinnedMatch?control.source.pinned_assets.find(asset=>canonicalAssetStem(asset.url)===canonicalAssetStem(row.url))?.key:`dc-page-${seeds.size+1}`,label:row.label||'Batman Returns (1992) page image',url:row.url,context:row.context,origin:row.origin})}catch{}}
  assert(seeds.size>=3,`official DC orbit retained only ${seeds.size} exact movie assets`);
  const attempted=[],usable=[],seenHashes=new Set();let index=0;
  for(const seed of seeds.values())for(const variant of variantUrls(seed)){const row=await download(context,seed,variant,++index);attempted.push(row);if(!row.sha256||row.download_error)continue;if(seenHashes.has(row.sha256)){row.visual_byte_duplicate=true;continue}seenHashes.add(row.sha256);row.repository_matches=repository.get(row.sha256)||[];usable.push(row)}
  assert(usable.length>=3,`official DC orbit produced only ${usable.length} usable unique byte candidates`);
  usable.sort((a,b)=>a.seed_key.localeCompare(b.seed_key)||(b.width*b.height-a.width*a.height)||a.probe_kind.localeCompare(b.probe_kind));
  const thumbs=[];
  for(let position=0;position<usable.length;position++){const row=usable[position],thumb=join(OUT,'thumbs',`${String(position+1).padStart(2,'0')}.jpg`);await mkdir(dirname(thumb),{recursive:true});magick(join(OUT,row.local),'-auto-orient','-thumbnail','400x300>','-background','#171512','-gravity','center','-extent','400x300','-fill','white','-undercolor','#171512cc','-gravity','south','-pointsize','12','-annotate','+0+4',`${String(position+1).padStart(2,'0')} ${row.seed_key} ${row.probe_kind} ${row.width}x${row.height}`,'-strip','-quality','88',thumb);thumbs.push(thumb)}
  const contact=join(OUT,'contact-sheet.jpg');execFileSync('montage',[...thumbs,'-tile','3x','-geometry','400x300+10+10','-background','#e8e3d9',contact],{stdio:'inherit'});
  const pageEvidence={navigation_status:navigation?.status()||null,page_title:title,resolved_page:page.url(),required_terms:control.source.required_terms,required_terms_missing:missingTerms,synopsis_evidence:'Official DC synopsis identifies the bizarre, sinister Penguin as Danny DeVito.',exact_asset_stems:[...seeds.values()].map(seed=>({key:seed.key,label:seed.label,stem:seed.stem,source_urls:seed.source_urls,origins:seed.origins}))};
  const manifest={version:1,lane:'card-backfill',record_id:'UC-069',actor:'Danny DeVito',character:'The Penguin',production:'Batman Returns',year:1992,side:'still',expected_subject:'The Penguin',generated_at:new Date().toISOString(),control_sha256:sha(await readFile(CONTROL)),selector_artifact:control.selector_artifact,repository_hash_count:repository.size,source:{provider:'DC',source_page:control.source.source_page,page_evidence,page_screenshot:{path:screenshot,sha256:sha(pageBytes)}},attempted,usable_candidates:usable,usable_candidate_count:usable.length,contact_sheet:{path:'contact-sheet.jpg',...identify(contact)},disposition:'candidate-only-pending-visual-selection',canonical_mutation:false};
  await writeJson(join(OUT,'manifest.json'),manifest);
  await writeJson(join(OUT,'summary.json'),{record_id:'UC-069',actor:'Danny DeVito',character:'The Penguin',seed_count:seeds.size,usable_candidate_count:usable.length,candidates:usable.map(({seed_key,seed_label,probe_kind,probe_url,resolved_url,local,mime,bytes,sha256,width,height,repository_matches})=>({seed_key,seed_label,probe_kind,probe_url,resolved_url,local,mime,bytes,sha256,width,height,repository_matches}))});
  const cards=usable.map((row,position)=>`<article><img src="${row.local}" alt=""><h2>${position+1} · ${row.seed_key} · ${row.probe_kind}</h2><p>${row.seed_label}</p><p>${row.width}×${row.height} · ${row.bytes} bytes</p><p>${row.repository_matches.length?`duplicate: ${row.repository_matches.join(', ')}`:'no exact canonical duplicate'}</p><code>${row.sha256}</code></article>`).join('');
  await writeFile(join(OUT,'review.html'),`<!doctype html><meta charset="utf-8"><style>body{font:14px system-ui;background:#e8e3d9;margin:24px}.sheet{max-width:100%}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:12px}article{background:white;padding:10px}article img{width:100%;height:340px;object-fit:contain;background:#171512}code{display:block;font-size:9px;word-break:break-all}</style><h1>UC-069 · Danny DeVito / Penguin official DC orbit</h1><p>Candidate-only. Approve only Danny DeVito's live-action 1992 Penguin; reject Batman, Catwoman, other cast, comics, animation, other Penguin actors, posters, logos, and gallery contamination.</p><img class="sheet" src="contact-sheet.jpg"><div class="grid">${cards}</div>`);
  console.log(`UC-069 official DC orbit: ${usable.length} usable unique byte candidate(s) from ${seeds.size} exact movie asset stems`);
  console.log(`source-page ${sha(pageBytes)}`);
  console.log(`contact ${contact}`);
  console.log(`artifact ${OUT}`);
}finally{await browser.close()}
