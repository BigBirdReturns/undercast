#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
import { spawnSync } from 'node:child_process';
const RECEIPT="data/review/adapter-sdk/star-trek-murf-cycle.json", CHECKER="scripts/star-trek-murf-cycle.mjs";
const TASK="ap_a55667678da96f42639931c7", LEASE="lease_eaaee76d6d197e160cfe8ace", WALL="UC-1373", CYCLE="cycle_40248cbf73afbe1269a19bd7", EVENT="waterline_fdb702a2b81ddd580aa93e51", REVIEW="2026-08-15T15:49:08.223Z";
const PERFORMER="Dee Bradley Baker", ROLE="Murf", SOURCE="https://memory-alpha.fandom.com/wiki/Murf", FIRST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Lost_and_Found_(episode)", LAST_EPISODE_SOURCE="https://memory-alpha.fandom.com/wiki/Ouroboros,_Part_II_(episode)", FINGERPRINT="2f5951f9f5ff141ac513fa3660aae0a843dac7ccdf3c9430c5fce948ab30e2e0";
const MAIN="cfc28dce9854445f00c02fb72c802b89bd9e5503", CANDIDATE="caadea6a20d7da1e71a9f27c53a7c7e5e82a05bb", TREE="f9ec1fbec6079388269873bcae7151b9acb7caf3", PATH_SHA="57437eca4217a196c9bea97bd0b20b1ab96f38c601217a124497002166839b13", REVIEW_ID="8dca24960ef3311d2526e75dbcdd78a65acf0bf15ac5953dbfa67863955412fb", REVIEW_FILE="8355b29f89acf256231f85c7ea49d49ee53c9bcd75858529141acea79863c1b4", PUBLICATION_RUN=31893733770, PUBLICATION_JOB=95033733280;
const STILL_PATH="images/uc-1373-still.webp", STILL_SHA="e4f6dcce06ea940bc50e8d932f40ea7640a9688f655c11afee5755764e0eb556", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/2/2e/Murf%2C_2385.png/revision/latest?cb=20240703142718&path-prefix=en", PORTRAIT_PATH="images/uc-1373-portrait.jpg", PORTRAIT_SHA="5572cbf8415316eb627aab6d215f182219047c48b96aeadbad81809c88f87478", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File:Dee_Bradley_Baker_by_Gage_Skidmore_2.jpg";
const JT_RECEIPT="data/review/adapter-sdk/star-trek-jamest-cycle.json", JT_RECEIPT_FILE="77756e281fc196e57de1f92105d27ed223e37c39dada41314756bd24e4cf1069", JT_RECEIPT_ID="8e463ba37572e7660eff90adddde40419e79b2f3301096a97278d511e509c56b", JT_CHECKER="scripts/star-trek-jamest-cycle.mjs", JT_CHECKER_SHA="32c7b9426bc7b0f3ac5cf28ec6a85f982b4a662241fed2a895ef4c70bd707aaf", JT_CYCLE="cycle_8e1ef98bc2b760f78837abb6";
const TOTAL=2228, RESOLVED_FLOOR=404;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-MURF'&&receipt.canonical_parent===MAIN,'Murf receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Murf checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Murf task drifted');
same(task.performance_modes,['voice-animation'],'Murf queued mode hint drifted');
same(task.wall_ids,[WALL],'Murf wall binding drifted');
ok(task.outcome?.review_sha256===receipt.canonical.outcome_review_sha256,'Murf task review binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='Star Trek: Prodigy'&&card.universe==='Star Trek'&&card.years==='2021–2024'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Murf canonical record drifted');
ok(card.reveal.includes('Murf’s vocal effects were provided by Dee Bradley Baker')&&card.reveal.includes('voice-only character-performance claim')&&card.reveal.includes('sound-processing credit')&&card.reveal.includes('holographic duplicate')&&card.reveal.includes('No physical performance')&&card.reveal.includes('animation')&&card.reveal.includes('production-shop'),'Murf performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===FIRST_EPISODE_SOURCE)&&card.references?.some(x=>x.claim==='production'&&x.source===LAST_EPISODE_SOURCE),'Murf source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Murf media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='CC BY-SA 3.0','Murf media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Murf source still drifted');
same(source.portrait,card.portrait,'Murf source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Murf facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Murf still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Murf portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='voice-animation'&&receipt.task?.species==='Mellanoid slime worm'&&receipt.task?.first_appearance==='Lost and Found'&&receipt.task?.last_appearance==='Ouroboros, Part II'&&receipt.task?.performance_scope==='character vocal effects and nonverbal vocalizations'&&receipt.task?.source_wording==="Murf's vocal effects were provided by Dee Bradley Baker."&&receipt.task?.vocal_effects_are_performance_not_processing_credit===true&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Murf adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Murf receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Murf claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Murf candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const murf=byLease.get(LEASE);
ok(murf?.id===CYCLE&&murf.outcome==='completed'&&murf.task_statuses?.[TASK]==='resolved'&&murf.reviewed_at===REVIEW,'Murf cycle receipt drifted');
const cb=structuredClone(murf);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Murf cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Murf waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Murf waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1823,resolved:404,blocked:0,rejected:1,in_flight:0},'Murf terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const prior=read(JT_RECEIPT), priorBody=structuredClone(prior);delete priorBody.receipt_sha256;
ok(sha(fs.readFileSync(JT_RECEIPT))===JT_RECEIPT_FILE&&prior.receipt_sha256===JT_RECEIPT_ID&&prior.receipt_sha256===sha(pretty(priorBody))&&prior.reviewed_cycle?.id===JT_CYCLE&&sha(fs.readFileSync(JT_CHECKER))===JT_CHECKER_SHA,'Murf lost prior JamesT custody');
const priorRun=spawnSync(process.execPath,[JT_CHECKER],{cwd:process.cwd(),encoding:'utf8',maxBuffer:256*1024*1024});
ok(priorRun.status===0,'JamesT checker failed under Murf custody: '+(priorRun.stdout||'')+(priorRun.stderr||''));
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Murf qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Murf execution custody drifted');
ok(read('package.json').scripts?.['star-trek:murf-cycle:check']==='node scripts/star-trek-murf-cycle.mjs','Murf checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.role_or_maker_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.animation_or_maker_attributed===false&&receipt.boundary?.vocal_effects_promoted_to_processing_credit===false&&receipt.boundary?.hologram_role_conflated===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.cross_facet_substitution===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Murf authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1373/'),'Murf permanent route missing');
console.log('star-trek-murf-cycle: PASS — exact Murf vocal-effects performance custody, exact independent character and performer media, no processing, physical, animation, or maker promotion, separate hologram-role custody, reviewed waterline closure, immutable JamesT custody, and later-cycle bounds are intact');
