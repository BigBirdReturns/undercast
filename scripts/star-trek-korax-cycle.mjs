#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';
const RECEIPT="data/review/adapter-sdk/star-trek-korax-cycle.json", CHECKER="scripts/star-trek-korax-cycle.mjs";
const TASK="ap_a926c74957211800c4a6d482", LEASE="lease_6745fd243478c16e4687f721", WALL="UC-1369", CYCLE="cycle_d0ac4d87953b8ff835c88fb9", EVENT="waterline_31e8a4693e71845b1909b0ac", REVIEW="2026-08-14T14:55:12.325Z";
const PERFORMER="James Doohan", ROLE="Korax", SOURCE="https://memory-alpha.fandom.com/wiki/Korax", FINGERPRINT="3b2d3f92e0602b3b07ee24cd900a03448ab355fbdfb1b35e2d78ca1d035bd4ca";
const MAIN="67c02172d3bdc231048e498c21aa421d59b48160", CANDIDATE="2c4df7ad33e4561722422c5721d29530273432e1", TREE="ae19229f0125acea58aca7177465d0d77fc75e37", PATH_SHA="4460a0b980d583303c87c331ca20cffed4b4d1eb7ada47f3a116bffaa57fd606", REVIEW_ID="6e4626851e72ccd823ea25678f9c2d34bf29d1aeadf42188d928b94ebac2b5c9", REVIEW_FILE="5da97c6e60374958ad1f13c6de2517fb54dfb8a86ce637ca22834cb8b5357668", PUBLICATION_RUN=31811801692, PUBLICATION_JOB=94803953606;
const STILL_PATH="images/uc-1369-still.webp", STILL_SHA="63fecf2b68b889bf324e3610775c081fe8ed21ec4f3d6896f60d280d982bc3dd", STILL_ORIGIN="https://static.wikia.nocookie.net/memoryalpha/images/b/b4/Korax%2C_2269.jpg/revision/latest?cb=20061209174834&path-prefix=en", PORTRAIT_PATH="images/uc-1369-portrait.jpg", PORTRAIT_SHA="81b7972b6e7d950b1384c50bfd5fd3f77910fc5c27b2691b87ae2ce4717edf99", PORTRAIT_ORIGIN="https://commons.wikimedia.org/wiki/File%3A%27Engineer_of_the_Future%27-_Star_Trek%27s_%27Scotty%2C%27_James_Doohan%2C_Presents_at_NASA_Goddard_%28James_Doohan_at_GSFC_1992%29.webm";
const TOTAL=2228, RESOLVED_FLOOR=400;
const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])])):v;
const sj=v=>JSON.stringify(stable(v)), pretty=v=>JSON.stringify(stable(v),null,2)+'\n';
const read=f=>JSON.parse(fs.readFileSync(f,'utf8')), jsonl=f=>fs.readFileSync(f,'utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
const ok=(x,m)=>{if(!x)throw Error(m)}, same=(a,b,m)=>ok(sj(a)===sj(b),m);
const receipt=read(RECEIPT), rb=structuredClone(receipt);delete rb.receipt_sha256;
ok(receipt.receipt_sha256===sha(pretty(rb))&&receipt.transaction==='STAR-TREK-CYCLE-KORAX'&&receipt.canonical_parent===MAIN,'Korax receipt identity drifted');
ok(receipt.qualification?.checker_sha256===sha(fs.readFileSync(CHECKER)),'Korax checker hash drifted');
const state=read('data/AUTOPILOT.json'), trek=state.jobs.filter(x=>x.scope==='star-trek'), task=trek.find(x=>x.id===TASK);
ok(trek.length===TOTAL&&task?.status==='resolved'&&task.performer===PERFORMER&&task.character===ROLE&&task.source_fingerprint===FINGERPRINT,'Korax task drifted');
same(task.performance_modes,['physical-and-voice'],'Korax queued mode hint drifted');
same(task.wall_ids,[WALL],'Korax wall binding drifted');
const card=read('data/specimens.json').find(x=>x.id===WALL);
ok(card&&card.actor===PERFORMER&&card.character===ROLE&&card.production==='More Tribbles, More Troubles'&&card.universe==='Star Trek'&&card.years==='1973'&&card.kind==='voice'&&card.transform===2&&card.designer==='—'&&card.link===SOURCE,'Korax canonical record drifted');
ok(card.reveal.includes('Michael Pataki')&&card.reveal.includes('Koloth')&&card.reveal.includes('role-specific voice contribution')&&card.reveal.includes('physical performance'),'Korax performance boundary drifted');
ok(card.references?.some(x=>x.claim==='performance'&&x.source===SOURCE)&&card.references?.some(x=>x.source.includes('More_Tribbles,_More_Troubles_(episode)')),'Korax source custody drifted');
ok(sha(fs.readFileSync(STILL_PATH))===STILL_SHA&&sha(fs.readFileSync(PORTRAIT_PATH))===PORTRAIT_SHA,'Korax media bytes drifted');
ok(card.still?.origin===STILL_ORIGIN&&card.portrait?.origin===PORTRAIT_ORIGIN&&card.portrait?.license==='Public domain','Korax media provenance drifted');
const source=read('data/SOURCES.json').find(x=>x.id===WALL);
same(source.still,card.still,'Korax source still drifted');
same(source.portrait,card.portrait,'Korax source portrait drifted');
const facets=read('data/MEDIA-AUDIT.json').items.filter(x=>x.wall_id===WALL).sort((a,b)=>a.side.localeCompare(b.side));
ok(facets.length===2&&facets.every(x=>x.status==='verified'),'Korax facets drifted');
const portrait=facets.find(x=>x.side==='portrait'), still=facets.find(x=>x.side==='still');
ok(still?.asset?.sha256===STILL_SHA&&still.claims?.identity?.value==='expected'&&still.claims?.presentation?.value==='character-depiction','Korax still review drifted');
ok(portrait?.asset?.sha256===PORTRAIT_SHA&&portrait.claims?.identity?.value==='expected'&&portrait.claims?.presentation?.value==='neutral-human','Korax portrait review drifted');
ok(receipt.task?.adjudicated_kind==='voice'&&receipt.task?.performance_mode==='voice-only'&&receipt.task?.queued_mode_hint?.[0]==='physical-and-voice'&&receipt.task?.maker_attribution==='unresolved'&&receipt.task?.vocal_transformation_measured===false,'Korax adjudication drifted');
ok(receipt.canonical.record_sha256===sha(pretty(card))&&receipt.media.facets_sha256===sha(pretty(facets))&&receipt.media.source_ledger_sha256===sha(pretty(source)),'Korax receipt projections drifted');
const claims=jsonl('data/journal/autopilot.jsonl').filter(x=>x.op==='lease.claimed'&&x.scope==='star-trek');
for(const row of claims){const b=structuredClone(row);delete b.id;ok(row.id==='apj_'+sha(JSON.stringify(b)).slice(0,24),'Star Trek claim is not content-addressed')}
const claim=claims.find(x=>x.lease_id===LEASE&&x.task_id===TASK);
ok(claim?.id===receipt.lease.claim_event_id,'Korax claim drifted');
const acceptance=jsonl('data/journal/candidates.jsonl').filter(x=>x.op==='draft.accept'&&x.specimen===WALL);
ok(acceptance.length===1&&acceptance[0].id===receipt.candidate.event_id,'Korax candidate event drifted');
const water=read('data/WATERLINE-STATE.json'), receipts=water.cycles.filter(x=>x.scope_id==='star-trek'), byLease=new Map();
for(const row of receipts){ok(!byLease.has(row.lease_id),'duplicate Star Trek cycle receipt');byLease.set(row.lease_id,row)}
const korax=byLease.get(LEASE);
ok(korax?.id===CYCLE&&korax.outcome==='completed'&&korax.task_statuses?.[TASK]==='resolved'&&korax.reviewed_at===REVIEW,'Korax cycle receipt drifted');
const cb=structuredClone(korax);delete cb.id;
ok(CYCLE==='cycle_'+sha(sj(cb)).slice(0,24),'Korax cycle is not content-addressed');
const events=jsonl('data/journal/waterline.jsonl').filter(x=>x.id===EVENT&&x.lease_id===LEASE&&x.receipt_id===CYCLE);
ok(events.length===1,'Korax waterline event drifted');
const eb=structuredClone(events[0]);delete eb.id;
ok(EVENT==='waterline_'+sha(JSON.stringify(eb)).slice(0,24),'Korax waterline event is not content-addressed');
const later=claims.filter(x=>Date.parse(x.at)>Date.parse(REVIEW)), unreceipted=later.filter(x=>!byLease.has(x.lease_id));
ok(unreceipted.length<=1,'more than one later Star Trek cycle is unreceipted');
ok(trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length<=1,'more than one later Star Trek task is active');
for(const row of later.filter(x=>byLease.has(x.lease_id))){const c=byLease.get(row.lease_id),j=trek.find(x=>x.id===row.task_id);ok(c.task_ids?.length===1&&c.task_ids[0]===row.task_id&&j?.status==='resolved','later receipted Star Trek cycle drifted')}
const resolved=trek.filter(x=>x.status==='resolved').length, queued=trek.filter(x=>x.status==='queued').length;
ok(resolved>=RESOLVED_FLOOR,'Star Trek resolved floor regressed');
if(later.length===0)same({total:trek.length,queued,resolved,blocked:trek.filter(x=>x.status==='blocked').length,rejected:trek.filter(x=>x.status==='rejected').length,in_flight:trek.filter(x=>['leased','drafted','merged'].includes(x.status)).length},{total:2228,queued:1827,resolved:400,blocked:0,rejected:1,in_flight:0},'Korax terminal queue drifted');
const latest=receipts.at(-1), registry=read('data/ESTATE-REGISTRY.json'), estate=registry.estates.find(x=>x.id==='star-trek'), registryQueued=queued+unreceipted.length;
ok(latest&&estate?.next_gate?.includes(latest.id)&&estate.next_gate.includes(registryQueued.toLocaleString('en-US')+' tasks remain queued'),'Star Trek registry phase gate drifted');
const baseline=read('data/review/adapter-sdk/BASELINE.json');
ok(baseline.inputs?.estate_registry?.sha256===sha(fs.readFileSync('data/ESTATE-REGISTRY.json')),'adapter baseline registry binding drifted');
const pc=receipt.prior_custody;
ok(sha(fs.readFileSync(pc.kor_receipt_path))===pc.kor_receipt_file_sha256&&read(pc.kor_receipt_path).receipt_sha256===pc.kor_receipt_identity&&sha(fs.readFileSync(pc.kor_checker_path))===pc.kor_checker_sha256,'Korax lost Kor custody');
ok(sha(fs.readFileSync(pc.koloth_receipt_path))===pc.koloth_receipt_file_sha256&&read(pc.koloth_receipt_path).receipt_sha256===pc.koloth_receipt_identity&&sha(fs.readFileSync(pc.koloth_checker_path))===pc.koloth_checker_sha256,'Korax lost Koloth custody');
ok(sha(fs.readFileSync(pc.kaz_receipt_path))===pc.kaz_receipt_file_sha256&&read(pc.kaz_receipt_path).receipt_sha256===pc.kaz_receipt_identity&&sha(fs.readFileSync(pc.kaz_checker_path))===pc.kaz_checker_sha256,'Korax lost Kaz custody');
ok(sha(fs.readFileSync(pc.guardian_receipt_path))===pc.guardian_receipt_file_sha256&&read(pc.guardian_receipt_path).receipt_sha256===pc.guardian_receipt_identity&&sha(fs.readFileSync(pc.guardian_checker_path))===pc.guardian_checker_sha256,'Korax lost Guardian custody');
ok(sha(fs.readFileSync(pc.landru_receipt_path))===pc.landru_receipt_file_sha256&&read(pc.landru_receipt_path).receipt_sha256===pc.landru_receipt_identity&&sha(fs.readFileSync(pc.landru_checker_path))===pc.landru_checker_sha256,'Korax lost Landru custody');
ok(sha(fs.readFileSync(pc.curzon_receipt_path))===pc.curzon_receipt_file_sha256&&read(pc.curzon_receipt_path).receipt_sha256===pc.curzon_receipt_identity&&sha(fs.readFileSync(pc.curzon_checker_path))===pc.curzon_checker_sha256,'Korax lost Curzon custody');
ok(sha(fs.readFileSync(pc.armus_receipt_path))===pc.armus_receipt_file_sha256&&read(pc.armus_receipt_path).receipt_sha256===pc.armus_receipt_identity&&sha(fs.readFileSync(pc.armus_checker_path))===pc.armus_checker_sha256&&sha(fs.readFileSync(pc.armus_historical_checker_path))===pc.armus_historical_checker_sha256,'Korax lost Armus custody');
ok(sha(fs.readFileSync(pc.m5_receipt_path))===pc.m5_receipt_file_sha256&&read(pc.m5_receipt_path).receipt_sha256===pc.m5_receipt_identity&&sha(fs.readFileSync(pc.m5_checker_path))===pc.m5_checker_sha256&&sha(fs.readFileSync(pc.m5_previous_wrapper_path))===pc.m5_previous_wrapper_sha256&&sha(fs.readFileSync(pc.m5_historical_checker_path))===pc.m5_historical_checker_sha256&&sha(fs.readFileSync(pc.m5_composability_receipt_path))===pc.m5_composability_receipt_file_sha256&&read(pc.m5_composability_receipt_path).receipt_sha256===pc.m5_composability_receipt_identity,'Korax lost M-5 custody');
const execution=structuredClone(receipt.execution), qualificationIdentity=execution.qualification_identity;delete execution.qualification_identity;
ok(qualificationIdentity===sha(sj(execution)),'Korax qualification identity drifted');
ok(receipt.execution?.publication_run===PUBLICATION_RUN&&receipt.execution?.publication_job===PUBLICATION_JOB&&receipt.execution?.candidate_commit===CANDIDATE&&receipt.execution?.candidate_tree===TREE&&receipt.execution?.candidate_path_ledger_sha256===PATH_SHA&&receipt.execution?.independent_review_identity===REVIEW_ID&&receipt.execution?.independent_review_file_sha256===REVIEW_FILE,'Korax execution custody drifted');
ok(read('package.json').scripts?.['star-trek:korax-cycle:check']==='node scripts/star-trek-korax-cycle.mjs','Korax checker route drifted');
ok(receipt.boundary?.queued_mode_hint_promoted===false&&receipt.boundary?.other_korax_performers_conflated===false&&receipt.boundary?.physical_performance_attributed===false&&receipt.boundary?.vocal_transformation_measured===false&&receipt.boundary?.role_specific_maker_attributed===false&&receipt.boundary?.outside_human_dependency===false&&receipt.boundary?.owner_physical_action_required===false&&receipt.boundary?.additional_lease_issued===false,'Korax authority boundary drifted');
ok(fs.readFileSync('sitemap.xml','utf8').includes('records/UC-1369/'),'Korax permanent route missing');
console.log('star-trek-korax-cycle: PASS — exact animated Korax voice custody, separate exact character and public-domain performer media, separate Michael Pataki physical-portrayal and Koloth role identity, and no physical-performance transfer, reviewed waterline closure, immutable Kor and prior-cycle custody, and later-cycle bounds are intact');
